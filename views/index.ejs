<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>KUMA Test Case Builder - ผู้ช่วยสร้างกรณีทดสอบ AI (ฅ^•ﻌ•^ฅ)</title>
    <meta name="description"
        content="KUMA Test Case Builder - เครื่องมือช่วยสร้าง Test Case และ JSON Payload อัตโนมัติด้วยพลัง AI น้องคุมะ ช่วยวิเคราะห์ Spec และสร้างขั้นตอนการทดสอบได้ทันที">
    <meta name="keywords"
        content="KUMA, Test Case Builder, Software Testing, AI Assistant, Automated Test Cases, JSON Payload Generator, ผู้ช่วยทดสอบซอฟต์แวร์">
    <meta name="author" content="KUMA Team">

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://cat-barank.onrender.com/">
    <meta property="og:title" content="KUMA Test Case Builder - ผู้ช่วยสร้างกรณีทดสอบ AI">
    <meta property="og:description"
        content="สร้าง Test Case และ JSON Payload ง่ายๆ ด้วยพลัง AI น้องคุมะ พร้อมส่งออกไฟล์ Excel ได้ทันที!">
    <meta property="og:image" content="https://cat-barank.onrender.com/favicon.ico">

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image">
    <meta property="twitter:url" content="https://cat-barank.onrender.com/">
    <meta property="twitter:title" content="KUMA Test Case Builder - ผู้ช่วยสร้างกรณีทดสอบ AI">
    <meta property="twitter:description" content="สร้าง Test Case และ JSON Payload ง่ายๆ ด้วยพลัง AI น้องคุมะ">

    <link rel="canonical" href="https://cat-barank.onrender.com/" />
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/exceljs@4.4.0/dist/exceljs.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/file-saver@2.0.5/dist/FileSaver.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Kanit', sans-serif;
            background-color: #fff1f2;
        }

        .cat-card {
            background-color: white;
            border: 4px solid #fda4af;
            border-radius: 2.5rem;
        }

        .btn-primary {
            background-color: #fda4af;
            color: white;
            transition: all 0.3s ease;
        }

        .btn-primary:hover:not(:disabled) {
            background-color: #f43f5e;
            transform: translateY(-2px);
        }

        .btn-primary:disabled {
            background-color: #fecdd3;
            cursor: not-allowed;
        }

        .btn-danger {
            background-color: #fee2e2;
            color: #ef4444;
            transition: all 0.2s ease;
        }

        .btn-danger:hover {
            background-color: #fecaca;
        }

        input,
        textarea {
            border: 2px solid #fecdd3;
            border-radius: 1rem;
            transition: border-color 0.3s ease;
        }

        input:focus,
        textarea:focus {
            outline: none;
            border-color: #fda4af;
            box-shadow: 0 0 0 3px rgba(253, 164, 175, 0.2);
        }

        .tab-active {
            background-color: #fda4af;
            color: white;
            box-shadow: 0 4px 15px rgba(253, 164, 175, 0.4);
        }

        .tab-inactive {
            background-color: white;
            color: #fda4af;
            border: 2px solid #fecdd3;
        }

        .tab-inactive:hover {
            background-color: #fff1f2;
        }

        .json-block {
            background-color: #1e293b;
            color: #e2e8f0;
            border-radius: 1rem;
            font-family: 'Courier New', monospace;
            font-size: 0.85rem;
            overflow-x: auto;
        }

        .json-key {
            color: #7dd3fc;
        }

        .json-string {
            color: #86efac;
        }

        .json-number {
            color: #fcd34d;
        }

        .json-bool {
            color: #c4b5fd;
        }

        .upload-zone {
            border: 3px dashed #fecdd3;
            border-radius: 1.5rem;
            transition: all 0.3s ease;
        }

        .upload-zone:hover,
        .upload-zone.drag-over {
            border-color: #fda4af;
            background-color: #fff1f2;
        }

        /* Animated Background Cats */
        #cat-atmosphere {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            pointer-events: none;
            overflow: hidden;
        }

        .bg-cat {
            position: absolute;
            opacity: 0.15;
            filter: grayscale(0.2);
            animation: float 20s infinite linear;
            user-select: none;
        }

        @keyframes float {
            0% {
                transform: translate(0, 0) rotate(0deg);
            }

            25% {
                transform: translate(15px, 25px) rotate(10deg);
            }

            50% {
                transform: translate(-10px, 40px) rotate(-5deg);
            }

            75% {
                transform: translate(-25px, 15px) rotate(5deg);
            }

            100% {
                transform: translate(0, 0) rotate(0deg);
            }
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(10px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* AI Sidebar Styles */
        #ai-sidebar {
            transition: transform 0.3s ease;
            z-index: 100;
        }

        .chat-bubble {
            border-radius: 1.5rem;
            padding: 0.75rem 1rem;
            max-width: 85%;
            margin-bottom: 0.5rem;
            font-size: 0.9rem;
            line-height: 1.4;
        }

        .chat-bubble.user {
            background-color: #fda4af;
            color: white;
            border-bottom-right-radius: 0.25rem;
            margin-left: auto;
        }

        .chat-bubble.ai {
            background-color: white;
            color: #4b5563;
            border: 2px solid #fecdd3;
            border-bottom-left-radius: 0.25rem;
            margin-right: auto;
        }

        .typing-dot {
            width: 6px;
            height: 6px;
            background: #fda4af;
            border-radius: 50%;
            display: inline-block;
            animation: typing 1s infinite alternate;
        }

        @keyframes typing {
            from {
                opacity: 0.3;
                transform: translateY(0);
            }

            to {
                opacity: 1;
                transform: translateY(-4px);
            }
        }

        /* Editable Table Styles */
        [contenteditable="true"] {
            border-bottom: 1px dashed transparent;
            padding: 2px 4px;
            border-radius: 4px;
            transition: all 0.2s;
        }

        [contenteditable="true"]:hover {
            background-color: #fff1f2;
            border-bottom-color: #fda4af;
        }

        [contenteditable="true"]:focus {
            outline: none;
            background-color: white;
            border: 2px solid #fda4af;
            box-shadow: 0 0 0 3px rgba(253, 164, 175, 0.2);
        }

        .file-preview-item {
            background: white;
            border: 1px solid #fecdd3;
            border-radius: 0.75rem;
            padding: 0.25rem 0.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.75rem;
            color: #e11d48;
            box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
        }

        .drag-over {
            background-color: #fff1f2 !important;
            border: 4px dashed #fda4af !important;
        }

        #chat-input {
            resize: none;
            max-height: 150px;
            min-height: 42px;
            overflow-y: hidden;
            line-height: 1.5;
        }

        /* ===== KUMA Desktop Pet ===== */
        #kuma-pet {
            position: fixed;
            bottom: 60px;
            left: 100px;
            z-index: 999;
            cursor: grab;
            user-select: none;
            transition: bottom 0.3s ease;
            filter: drop-shadow(2px 4px 6px rgba(0, 0, 0, 0.15));
        }

        #kuma-pet.dragging {
            cursor: grabbing;
            transition: none;
        }

        #kuma-pet.hidden-cat {
            bottom: -120px !important;
            opacity: 0;
            pointer-events: none;
            transition: all 0.5s ease;
        }

        #kuma-pet.entering {
            animation: catEnter 0.6s ease forwards;
        }

        @keyframes catEnter {
            0% {
                bottom: -120px;
                opacity: 0;
            }

            60% {
                bottom: 70px;
                opacity: 1;
            }

            80% {
                bottom: 55px;
            }

            100% {
                bottom: 60px;
                opacity: 1;
            }
        }

        /* Cat Body - Pure CSS Orange Cat */
        .kuma-body {
            position: relative;
            width: 70px;
            height: 55px;
        }

        /* Main body */
        .kuma-body .body {
            position: absolute;
            bottom: 0;
            left: 10px;
            width: 50px;
            height: 35px;
            background: #F4A236;
            border-radius: 50% 50% 45% 45%;
        }

        .kuma-body .body::after {
            content: '';
            position: absolute;
            top: 6px;
            left: 8px;
            width: 34px;
            height: 18px;
            background: #F7C16B;
            border-radius: 50%;
        }

        /* Head */
        .kuma-body .head {
            position: absolute;
            bottom: 22px;
            left: 0;
            width: 34px;
            height: 30px;
            background: #F4A236;
            border-radius: 50% 50% 40% 40%;
            z-index: 2;
        }

        /* Eyes */
        .kuma-body .eye {
            position: absolute;
            width: 6px;
            height: 7px;
            background: #333;
            border-radius: 50%;
            top: 12px;
            z-index: 3;
        }

        .kuma-body .eye.left {
            left: 8px;
        }

        .kuma-body .eye.right {
            left: 20px;
        }

        .kuma-body .eye::after {
            content: '';
            position: absolute;
            width: 2px;
            height: 2px;
            background: white;
            border-radius: 50%;
            top: 1px;
            left: 1px;
        }

        /* Nose */
        .kuma-body .nose {
            position: absolute;
            width: 5px;
            height: 3px;
            background: #E8836B;
            border-radius: 50%;
            top: 17px;
            left: 14px;
            z-index: 3;
        }

        /* Ears */
        .kuma-body .ear {
            position: absolute;
            width: 0;
            height: 0;
            border-left: 7px solid transparent;
            border-right: 7px solid transparent;
            border-bottom: 14px solid #F4A236;
            z-index: 1;
        }

        .kuma-body .ear.left {
            top: -6px;
            left: 1px;
            transform: rotate(-15deg);
        }

        .kuma-body .ear.right {
            top: -6px;
            left: 18px;
            transform: rotate(15deg);
        }

        .kuma-body .ear::after {
            content: '';
            position: absolute;
            border-left: 4px solid transparent;
            border-right: 4px solid transparent;
            border-bottom: 9px solid #FFD4A8;
            top: 4px;
            left: -4px;
        }

        /* Tail */
        .kuma-body .tail {
            position: absolute;
            bottom: 15px;
            right: -5px;
            width: 25px;
            height: 8px;
            background: #F4A236;
            border-radius: 0 50% 50% 0;
            transform-origin: left center;
            animation: tailWag 0.8s ease-in-out infinite alternate;
        }

        @keyframes tailWag {
            from {
                transform: rotate(-10deg);
            }

            to {
                transform: rotate(20deg);
            }
        }

        /* Legs */
        .kuma-body .leg {
            position: absolute;
            bottom: -8px;
            width: 8px;
            height: 12px;
            background: #E8952F;
            border-radius: 0 0 3px 3px;
        }

        .kuma-body .leg.fl {
            left: 14px;
        }

        .kuma-body .leg.fr {
            left: 24px;
        }

        .kuma-body .leg.bl {
            left: 38px;
        }

        .kuma-body .leg.br {
            left: 48px;
        }

        /* Walking animation */
        #kuma-pet.walking .leg.fl,
        #kuma-pet.walking .leg.br {
            animation: legWalk 0.3s ease-in-out infinite alternate;
        }

        #kuma-pet.walking .leg.fr,
        #kuma-pet.walking .leg.bl {
            animation: legWalk 0.3s ease-in-out infinite alternate-reverse;
        }

        @keyframes legWalk {
            from {
                height: 12px;
                bottom: -8px;
            }

            to {
                height: 8px;
                bottom: -4px;
            }
        }

        /* Sleeping animation */
        #kuma-pet.sleeping .eye {
            height: 2px !important;
            top: 15px !important;
            border-radius: 2px !important;
        }

        #kuma-pet.sleeping .zzz {
            display: block;
        }

        .zzz {
            display: none;
            position: absolute;
            top: -15px;
            right: 5px;
            font-size: 12px;
            color: #fda4af;
            animation: zzzFloat 1.5s ease-in-out infinite;
        }

        @keyframes zzzFloat {
            0% {
                opacity: 0;
                transform: translateY(0) scale(0.8);
            }

            50% {
                opacity: 1;
                transform: translateY(-8px) scale(1);
            }

            100% {
                opacity: 0;
                transform: translateY(-16px) scale(0.6);
            }
        }

        /* Rosy cheeks and mouth */
        .kuma-body .blush {
            position: absolute;
            width: 5px;
            height: 3px;
            background: rgba(244, 63, 94, 0.4); /* rose-500 pink blush */
            border-radius: 50%;
            top: 17px;
            z-index: 3;
        }
        .kuma-body .blush.left {
            left: 5px;
        }
        .kuma-body .blush.right {
            left: 24px;
        }
        
        .kuma-body .mouth {
            position: absolute;
            width: 6px;
            height: 4px;
            border: 1.5px solid #c2410c;
            border-top: none;
            border-radius: 0 0 3px 3px;
            top: 18px;
            left: 14px;
            z-index: 3;
        }

        /* Dizzy eyes when dragging */
        #kuma-pet.dragging .eye {
            height: 5px !important;
            width: 5px !important;
            border-radius: 50% !important;
            background: transparent !important;
            border: 1.5px solid #333 !important;
        }
        
        /* Happy state closed eyes */
        #kuma-pet.happy .eye {
            height: 3px !important;
            border-radius: 3px 3px 0 0 !important;
            background: transparent !important;
            border: 1.5px solid #333 !important;
            border-bottom: none !important;
            top: 14px !important;
        }

        /* Happy bounce animation on body */
        #kuma-pet.happy .kuma-body {
            animation: happyBounce 0.5s ease-in-out 2;
        }
        
        @keyframes happyBounce {
            0%, 100% { transform: translateY(0) scaleY(1); }
            50% { transform: translateY(-12px) scaleY(0.9) scaleX(1.05); }
        }

        /* Speech bubble */
        .kuma-bubble {
            position: absolute;
            top: -45px;
            left: 50%;
            transform: translateX(-50%);
            background: white;
            color: #db2777; /* pink-600 */
            border: 2px solid #fecdd3; /* rose-200 */
            padding: 4px 10px;
            border-radius: 12px;
            font-size: 11px;
            font-weight: 600;
            white-space: nowrap;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.08);
            opacity: 0;
            pointer-events: none;
            transition: all 0.3s ease;
            z-index: 9999;
        }
        .kuma-bubble::after {
            content: '';
            position: absolute;
            bottom: -6px;
            left: 50%;
            transform: translateX(-50%);
            border-width: 6px 6px 0;
            border-style: solid;
            border-color: white transparent;
            display: block;
            width: 0;
        }
        .kuma-bubble::before {
            content: '';
            position: absolute;
            bottom: -8px;
            left: 50%;
            transform: translateX(-50%);
            border-width: 8px 8px 0;
            border-style: solid;
            border-color: #fecdd3 transparent;
            display: block;
            width: 0;
            z-index: -1;
        }
        #kuma-pet.saying .kuma-bubble {
            opacity: 1;
            transform: translateX(-50%) translateY(-5px);
        }

        /* Cat House Button */
        #cat-house-btn {
            position: fixed;
            top: 65px;
            right: 15px;
            z-index: 1000;
            width: 48px;
            height: 48px;
            border-radius: 50%;
            background: linear-gradient(135deg, #F4A236, #E8952F);
            border: 3px solid #fff;
            box-shadow: 0 4px 15px rgba(244, 162, 54, 0.4);
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            font-size: 22px;
            transition: all 0.3s ease;
        }

        #cat-house-btn:hover {
            transform: scale(1.15);
            box-shadow: 0 6px 20px rgba(244, 162, 54, 0.6);
        }

        #cat-house-btn .tooltip {
            position: absolute;
            top: 55px;
            right: 0;
            transform: none;
            background: #333;
            color: white;
            padding: 4px 10px;
            border-radius: 8px;
            font-size: 11px;
            white-space: nowrap;
            opacity: 0;
            pointer-events: none;
            transition: opacity 0.2s;
        }

        #cat-house-btn:hover .tooltip {
            opacity: 1;
        }

        /* ===== Fancy Header Effects ===== */
        .kuma-title {
            font-size: 2.2rem;
            font-weight: 800;
            background: linear-gradient(90deg, #ff6b9d, #f4a236, #ff6b9d, #c084fc, #ff6b9d);
            background-size: 300% 100%;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: gradientShift 4s ease infinite, titleFloat 3s ease-in-out infinite;
            filter: drop-shadow(0 2px 8px rgba(255, 107, 157, 0.3));
            position: relative;
            display: inline-block;
        }

        .kuma-title:hover {
            animation: gradientShift 1s ease infinite, titleBounce 0.5s ease;
            filter: drop-shadow(0 4px 15px rgba(255, 107, 157, 0.5));
        }

        @keyframes gradientShift {
            0% {
                background-position: 0% 50%;
            }

            50% {
                background-position: 100% 50%;
            }

            100% {
                background-position: 0% 50%;
            }
        }

        @keyframes titleFloat {

            0%,
            100% {
                transform: translateY(0);
            }

            50% {
                transform: translateY(-4px);
            }
        }

        @keyframes titleBounce {

            0%,
            100% {
                transform: scale(1);
            }

            30% {
                transform: scale(1.05);
            }

            60% {
                transform: scale(0.97);
            }
        }

        .kuma-subtitle {
            font-size: 1rem;
            font-weight: 600;
            background: linear-gradient(90deg, #f4a236, #fcd34d, #f4a236);
            background-size: 200% 100%;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: shimmer 3s ease-in-out infinite;
            position: relative;
            display: inline-block;
            letter-spacing: 0.5px;
        }

        @keyframes shimmer {
            0% {
                background-position: -200% 0;
            }

            100% {
                background-position: 200% 0;
            }
        }

        /* Sparkle particles */
        .sparkle-container {
            position: relative;
            display: inline-block;
        }

        .sparkle {
            position: absolute;
            width: 6px;
            height: 6px;
            border-radius: 50%;
            background: #fcd34d;
            pointer-events: none;
            animation: sparkleAnim 2s ease-in-out infinite;
        }

        .sparkle:nth-child(1) {
            top: -8px;
            left: 10%;
            animation-delay: 0s;
            background: #ff6b9d;
        }

        .sparkle:nth-child(2) {
            top: -5px;
            right: 15%;
            animation-delay: 0.5s;
            background: #c084fc;
        }

        .sparkle:nth-child(3) {
            top: 0px;
            left: 50%;
            animation-delay: 1s;
            background: #fcd34d;
        }

        .sparkle:nth-child(4) {
            top: -10px;
            right: 30%;
            animation-delay: 1.5s;
            background: #f4a236;
        }

        @keyframes sparkleAnim {

            0%,
            100% {
                opacity: 0;
                transform: scale(0) translateY(0);
            }

            50% {
                opacity: 1;
                transform: scale(1) translateY(-10px);
            }
        }

        .cat-kaomoji {
            font-size: 2.8rem;
            display: inline-block;
            animation: kaomojiBounce 2s ease-in-out infinite;
            filter: drop-shadow(0 3px 6px rgba(253, 164, 175, 0.4));
        }

        @keyframes kaomojiBounce {

            0%,
            100% {
                transform: rotate(0deg) scale(1);
            }

            25% {
                transform: rotate(-5deg) scale(1.05);
            }

            75% {
                transform: rotate(5deg) scale(1.05);
            }
        }
    </style>
</head>

<body class="min-h-screen pb-20 overflow-x-hidden relative">
    <!-- AI Sidebar -->
    <div id="ai-sidebar"
        class="fixed left-0 top-0 h-full w-80 bg-white border-r-4 border-rose-200 shadow-2xl flex flex-col -translate-x-full">
        <!-- Toggle Button (Inside) -->
        <button onclick="toggleAI()"
            class="absolute -right-12 top-1/2 -translate-y-1/2 bg-white border-4 border-l-0 border-rose-200 p-2 rounded-r-2xl shadow-lg hover:bg-rose-50 transition-colors group">
            <span id="ai-toggle-icon" class="text-2xl block group-hover:scale-110 transition-transform">🐱</span>
        </button>

        <!-- Header -->
        <div class="p-6 border-b-2 border-rose-100 bg-rose-50/50">
            <h3 class="font-bold text-rose-500 flex items-center gap-2">
                <span class="text-2xl">🐱</span> ผู้ช่วย KUMA (คุมะ)
            </h3>
            <p class="text-xs text-rose-300 mt-1">สั่งให้คุมะสร้าง Test Case หรือถามอะไรก็ได้นะแง้ว!</p>
        </div>

        <!-- Chat Content -->
        <div id="chat-container" class="flex-1 overflow-y-auto p-4 space-y-4 bg-rose-50/20">
            <div class="chat-bubble ai shadow-sm">
                สวัสดีครับเมี้ยว มนุษย์! 🐾 ผมเป็นเอเจนท์น้องคุมะ ยินดีที่ได้รู้จักนะเมี้ยว สั่งผมได้เลย เช่น
                "สร้างเทสเคสสมัครสมาชิกให้หน่อย"
            </div>
        </div>

        <!-- Preview Area -->
        <div id="ai-file-preview" class="hidden flex-wrap gap-2 p-3 bg-rose-50/50 border-t border-rose-100"></div>

        <!-- Input -->
        <div class="p-4 border-t-2 border-rose-100 bg-white">
            <div class="flex gap-2 items-center">
                <button onclick="document.getElementById('ai-file-input').click()"
                    class="text-rose-400 hover:text-rose-500 transition-colors" title="แนบไฟล์">
                    <span class="text-2xl">📎</span>
                </button>
                <input type="file" id="ai-file-input" class="hidden" multiple onchange="handleAIFileSelect(this)">
                <textarea id="chat-input" placeholder="พิมพ์คุยหรือส่งไฟล์หาผม..."
                    class="flex-1 p-2 text-sm border-2 border-rose-50 rounded-xl focus:border-rose-300 focus:outline-none transition-all"
                    rows="1"></textarea>
                <button onclick="sendMessage()"
                    class="btn-primary p-2 rounded-xl h-[42px] flex items-center justify-center">
                    <span class="text-xl">🚀</span>
                </button>
            </div>
        </div>
    </div>
    <!-- Cat Atmosphere Layer -->
    <div id="cat-atmosphere"></div>
    <div class="max-w-4xl mx-auto">

        <!-- Header -->
        <div class="text-center mb-6">
            <div class="cat-kaomoji mb-2 text-rose-400">ฅ^•ﻌ•^ฅ</div>
            <div class="sparkle-container">
                <span class="sparkle"></span>
                <span class="sparkle"></span>
                <span class="sparkle"></span>
                <span class="sparkle"></span>
                <h1 class="kuma-title">เครื่องมือผู้ช่วย QA น้องคุมะ</h1>
            </div>
            <p class="kuma-subtitle mt-2">🐾 น้องคุมะ(KUMA) ผู้แข็งแกร่งที่สุดในโลก เมี้ยว 🐾</p>
        </div>

        <!-- Navigation Tabs -->
        <div class="flex justify-center gap-3 mb-4 flex-wrap">
            <button id="tabPageHome" onclick="switchPage('home')"
                class="tab-active px-6 py-3 rounded-full font-bold text-sm transition-all">
                🏠 หน้าหลัก
            </button>
            <button id="tabPage1" onclick="switchPage(1)"
                class="tab-inactive px-6 py-3 rounded-full font-bold text-sm transition-all">
                📝 สร้างกรณีทดสอบ
            </button>
            <button id="tabPage2" onclick="switchPage(2)"
                class="tab-inactive px-6 py-3 rounded-full font-bold text-sm transition-all">
                🔧 JSON Payload Generator
            </button>
            <button id="tabPage3" onclick="switchPage(3)"
                class="tab-inactive px-6 py-3 rounded-full font-bold text-sm transition-all">
                📊 Test Coverage Matrix
            </button>
            
            <!-- GISX Automate Dropdown Menu -->
            <div class="relative inline-block text-left group">
                <button id="tabPageGisxDropdown"
                    class="tab-inactive px-6 py-3 rounded-full font-bold text-sm transition-all inline-flex items-center gap-1 focus:outline-none">
                    🤖 GISX Automate <span class="text-xs">▼</span>
                </button>
                <div class="absolute right-0 mt-2 w-48 rounded-2xl shadow-xl bg-white z-50 hidden group-hover:block border-2 border-rose-100 overflow-hidden">
                    <div class="py-1">
                        <a href="#" onclick="switchPage('gisx_register_case'); return false;"
                            class="group flex items-center px-4 py-3 text-sm text-gray-700 hover:bg-rose-50 hover:text-rose-500 font-semibold transition-colors">
                            📝 สร้าง Register Case
                        </a>
                    </div>
                </div>
            </div>
        </div>


        <!-- ==================== PAGE HOME: Home Page with YouTube ==================== -->
        <div id="pageHome">
            <div class="cat-card p-8 shadow-2xl relative mb-10 text-center">
                <h2 class="text-2xl font-bold text-rose-500 mb-2 flex items-center justify-center gap-2">
                    📺 ค้นหาและดูวิดีโอ YouTube ในนี้ได้เลย!
                </h2>
                <p class="text-sm text-gray-500 mb-6">
                    คุณแม่มนุษย์สามารถพิมพ์ค้นหาเพลง คลิป หรืออะไรก็ได้บน YouTube แล้วเปิดเล่นสดผ่านเครื่องเล่นด้านล่างนี้ได้ทันทีเลยเมี้ยว! 🐾
                </p>

                <!-- YouTube Search Box -->
                <div class="max-w-xl mx-auto mb-6">
                    <div class="flex gap-2">
                        <input type="text" id="ytSearchInput" placeholder="🔍 ค้นหาวิดีโอ เช่น เพลงเพราะๆ, สอนเขียนโค้ด, การ์ตูน..." 
                            class="flex-1 px-4 py-2 border-2 border-rose-50 rounded-xl text-sm focus:border-rose-300 focus:outline-none transition-all"
                            onkeydown="if(event.key==='Enter') searchYoutube()">
                        <button onclick="searchYoutube()" 
                            class="bg-rose-500 text-white px-6 py-2 rounded-xl text-sm font-medium hover:bg-rose-600 transition-colors shadow-md flex items-center gap-1">
                            ค้นหา 🔍
                        </button>
                    </div>
                </div>

                <!-- YouTube Player Container -->
                <div class="relative w-full overflow-hidden rounded-2xl border-4 border-rose-100 shadow-lg mx-auto" style="padding-top: 56.25%; max-width: 720px;">
                    <iframe id="ytPlayer" class="absolute top-0 left-0 w-full h-full"
                        src="https://www.youtube.com/embed/1Jgq2Z_8W-c" 
                        title="YouTube video player" frameborder="0" 
                        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" 
                        allowfullscreen>
                    </iframe>
                </div>

                <!-- YouTube Search Results -->
                <div id="ytResults" class="grid grid-cols-2 sm:grid-cols-3 gap-4 mt-6 max-h-[300px] overflow-y-auto pr-2 hidden">
                    <!-- Dynamic Search Results -->
                </div>

                <!-- 3 New Quick Menus (Fortune, Offline Game, Online Game) -->
                <div class="flex justify-center gap-4 mt-8 flex-wrap border-t border-rose-50 pt-6">
                    <button onclick="toggleHomeSection('fortune')" class="bg-gradient-to-r from-purple-400 to-indigo-500 hover:from-purple-500 hover:to-indigo-600 text-white font-bold px-6 py-3 rounded-2xl shadow-md transition-all flex items-center gap-2 hover:-translate-y-0.5">
                        🔮 ดูดวงกับน้องคุมะ
                    </button>
                    <button onclick="toggleHomeSection('offlineGame')" class="bg-gradient-to-r from-amber-400 to-orange-500 hover:from-amber-500 hover:to-orange-600 text-white font-bold px-6 py-3 rounded-2xl shadow-md transition-all flex items-center gap-2 hover:-translate-y-0.5">
                        🎮 เล่นเกมออฟไลน์
                    </button>
                    <button onclick="toggleHomeSection('onlineGame')" class="bg-gradient-to-r from-teal-400 to-emerald-500 hover:from-teal-500 hover:to-emerald-600 text-white font-bold px-6 py-3 rounded-2xl shadow-md transition-all flex items-center gap-2 hover:-translate-y-0.5">
                        🌐 เล่นเกมออนไลน์
                    </button>
                </div>

                <!-- Interactive Sections (Initially Hidden) -->
                <div id="homeSectionsContainer" class="mt-8 max-w-2xl mx-auto hidden">
                    
                    <!-- Section 1: Fortune Telling -->
                    <div id="sectionFortune" class="bg-white border-2 border-purple-100 rounded-3xl p-6 shadow-xl relative hidden text-center animate-fade-in">
                        <h3 class="text-xl font-bold text-purple-600 mb-2 flex items-center justify-center gap-1">🔮 ดูดวงความแม่นยำสูงกับน้องคุมะ</h3>
                        <p class="text-xs text-gray-400 mb-4">อยากรู้อนาคตเรื่องอะไร ถามคุมะได้เลยเมี้ยว~</p>
                        
                        <div class="flex gap-2 max-w-xs mx-auto mb-4">
                            <select id="fortuneCategory" class="flex-1 px-4 py-2 border-2 border-purple-50 rounded-xl text-sm focus:border-purple-300 focus:outline-none bg-white">
                                <option value="love">❤️ ความรักความสัมพันธ์</option>
                                <option value="work">💼 การเรียนและการงาน</option>
                                <option value="money">💰 การเงินและโชคลาภ</option>
                                <option value="health">🍀 สุขภาพร่างกาย</option>
                            </select>
                            <button onclick="getFortune()" class="bg-purple-500 text-white px-4 py-2 rounded-xl text-sm font-semibold hover:bg-purple-600 transition-colors shadow">
                                ทำนายดวง
                            </button>
                        </div>
                        
                        <!-- Prediction Display Area -->
                        <div id="fortuneResult" class="hidden bg-purple-50/50 border border-purple-100/50 rounded-2xl p-4 mt-4 text-left relative min-h-[100px] flex flex-col justify-center">
                            <!-- Dynamic Content -->
                        </div>
                    </div>

                    <!-- Section 2: Offline Game (Catch the Fish) -->
                    <div id="sectionOfflineGame" class="bg-white border-2 border-amber-100 rounded-3xl p-6 shadow-xl hidden text-center animate-fade-in">
                        <h3 class="text-xl font-bold text-amber-600 mb-1 flex items-center justify-center gap-1">🐟 เกมคุมะตะปบปลาทู (Offline)</h3>
                        <p class="text-xs text-gray-400 mb-4">ใช้เมาส์หรือการทัชขยับคุมะซ้ายขวาเพื่อจับปลา ระวังลูกระเบิดและศัตรูนะแง้ว! 💣</p>
                        
                        <div class="flex justify-between items-center mb-2 max-w-[400px] mx-auto text-sm font-bold text-gray-700">
                            <div>คะแนน: <span id="gameScore">0</span></div>
                            <div>ชีวิต: <span id="gameLives" class="text-red-500">❤️❤️❤️</span></div>
                        </div>
                        
                        <canvas id="offlineGameCanvas" width="400" height="400" class="border-4 border-amber-100 rounded-2xl bg-amber-50/30 mx-auto max-w-full cursor-none shadow-md"></canvas>
                        
                        <div class="mt-4">
                            <button id="startGameBtn" onclick="initOfflineGame()" class="bg-amber-500 text-white font-bold px-6 py-2 rounded-xl hover:bg-amber-600 transition-colors shadow">
                                เริ่มเกม 🎮
                            </button>
                        </div>
                    </div>

                    <!-- Section 3: Online Game (Iframe embed list) -->
                    <div id="sectionOnlineGame" class="bg-white border-2 border-teal-100 rounded-3xl p-6 shadow-xl hidden text-center animate-fade-in">
                        <h3 class="text-xl font-bold text-teal-600 mb-1 flex items-center justify-center gap-1">🌐 ศูนย์รวมเกมออนไลน์สุดฮิต</h3>
                        <p class="text-xs text-gray-400 mb-4">เลือกเกมที่อยากเล่นด้านล่างแล้วสนุกกันเลยเมี้ยว~</p>
                        
                        <div class="flex justify-center gap-2 mb-4">
                            <button onclick="loadOnlineGame('2048')" class="bg-teal-500 text-white text-xs font-semibold px-4 py-2 rounded-lg hover:bg-teal-600 transition-colors shadow">2048 🔢</button>
                            <button onclick="loadOnlineGame('hextris')" class="bg-teal-500 text-white text-xs font-semibold px-4 py-2 rounded-lg hover:bg-teal-600 transition-colors shadow">Hextris 📐</button>
                            <button onclick="loadOnlineGame('pacman')" class="bg-teal-500 text-white text-xs font-semibold px-4 py-2 rounded-lg hover:bg-teal-600 transition-colors shadow">Pac-Man 🟡</button>
                        </div>
                        
                        <div class="relative w-full overflow-hidden rounded-2xl border-4 border-teal-50 shadow-md mx-auto" style="height: 480px; max-width: 480px;">
                            <iframe id="onlineGameIframe" class="absolute top-0 left-0 w-full h-full"
                                src="" 
                                frameborder="0" 
                                allowfullscreen>
                            </iframe>
                            <div id="onlineGameEmptyState" class="absolute inset-0 bg-teal-50/50 flex flex-col items-center justify-center text-teal-500">
                                <div class="text-5xl mb-2">🌐</div>
                                กรุณาเลือกเกมด้านบนเพื่อเริ่มเล่นครับเมี้ยว!
                            </div>
                        </div>
                    </div>

                </div>
            </div>
        </div>

        <!-- ==================== PAGE 1: Test Case Builder ==================== -->
        <div id="page1" class="hidden">
            <div class="cat-card p-8 shadow-2xl relative mb-10">
                <div class="space-y-4">
                    <div>
                        <label for="testCaseName" class="block text-sm font-medium text-rose-600 mb-1">ชื่อกรณีทดสอบ
                            (Test Case Name)</label>
                        <input type="text" id="testCaseName" placeholder="เช่น การล็อกอินเข้าสู่ระบบ"
                            class="w-full p-3 bg-rose-50">
                    </div>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <div>
                            <label for="testStep" class="block text-sm font-medium text-rose-600 mb-1">ขั้นตอนการทดสอบ
                                (Test Step)</label>
                            <textarea id="testStep" rows="3" placeholder="กรอกขั้นตอนการทดสอบที่นี่..."
                                class="w-full p-3 bg-rose-50 resize-none"></textarea>
                        </div>
                        <div>
                            <label for="expectedResult"
                                class="block text-sm font-medium text-rose-600 mb-1">ผลลัพธ์ที่คาดหวัง (Expected
                                Result)</label>
                            <textarea id="expectedResult" rows="3" placeholder="กรอกผลลัพธ์ที่คาดหวังที่นี่..."
                                class="w-full p-3 bg-rose-50 resize-none"></textarea>
                        </div>
                    </div>
                    <div class="flex justify-end">
                        <button id="addStepBtn" disabled class="btn-primary px-8 py-3 rounded-full font-bold shadow-lg">
                            เพิ่มขั้นตอนถัดไป (Add Next Step)
                        </button>
                    </div>
                </div>
            </div>

            <!-- Preview Table -->
            <div class="bg-white rounded-[2rem] p-6 shadow-xl border-2 border-rose-100">
                <div class="flex justify-between items-center mb-6">
                    <h2 class="text-xl font-bold text-rose-500 flex items-center">
                        <span class="mr-2">📋</span> ตารางตัวอย่าง (Preview Table)
                    </h2>
                    <div class="flex space-x-2">
                        <a href="/api/testcase-template"
                            class="bg-blue-50 text-blue-500 px-4 py-2 rounded-xl text-sm font-medium hover:bg-blue-100 transition-colors flex items-center">
                            📥 โหลดตัวอย่าง
                        </a>
                        <button onclick="document.getElementById('importTestCaseInput').click()"
                            class="bg-purple-50 text-purple-500 px-4 py-2 rounded-xl text-sm font-medium hover:bg-purple-100 transition-colors">
                            📂 นำเข้า Excel
                        </button>
                        <input type="file" id="importTestCaseInput" accept=".xlsx" class="hidden"
                            onchange="importTestCases(this)">
                        <button onclick="saveTestCaseHistory()"
                            class="bg-blue-100 text-blue-600 px-4 py-2 rounded-xl text-sm font-medium hover:bg-blue-200 transition-colors">
                            💾 บันทึกประวัติ
                        </button>
                        <button id="resetBtn"
                            class="bg-rose-50 text-rose-400 px-4 py-2 rounded-xl text-sm font-medium hover:bg-rose-100 transition-colors">
                            ล้างทั้งหมด
                        </button>
                        <button id="copySheetsBtn" onclick="copyTestCasesForGoogleSheets()"
                            class="bg-blue-50 text-blue-500 px-4 py-2 rounded-xl text-sm font-medium hover:bg-blue-100 transition-colors hidden"
                            title="คัดลอกเป็นแบบตารางสำหรับวางใน Google Sheets (Ctrl+V) ได้ทันที">
                            🟢 คัดลอกไป Google Sheets
                        </button>
                        <button id="exportBtn"
                            class="bg-green-100 text-green-600 px-4 py-2 rounded-xl text-sm font-medium hover:bg-green-200 transition-colors hidden">
                            ส่งออก Excel
                        </button>
                    </div>
                </div>
                <div class="overflow-x-auto">
                    <table class="w-full text-left">
                        <thead>
                            <tr class="border-b-2 border-rose-50">
                                <th class="py-3 px-4 text-rose-400 font-semibold w-16">ลำดับ</th>
                                <th class="py-3 px-4 text-rose-400 font-semibold">ชื่อกรณีทดสอบ</th>
                                <th class="py-3 px-4 text-rose-400 font-semibold">ขั้นตอน</th>
                                <th class="py-3 px-4 text-rose-400 font-semibold">ผลลัพธ์ที่คาดหวัง</th>
                                <th class="py-3 px-4 text-rose-400 font-semibold w-20 text-center">จัดการ</th>
                            </tr>
                        </thead>
                        <tbody id="testCaseTableBody" class="text-gray-600"></tbody>
                    </table>
                    <div id="emptyState" class="text-center py-10 text-rose-200">
                        <div class="text-5xl mb-2">🐾</div>
                        ยังไม่มีขั้นตอนการทดสอบ เพิ่มข้อมูลด้านบนเพื่อเริ่มสร้าง!
                    </div>
                </div>
            </div>
            <!-- Page 1 History -->
            <div class="mt-8 bg-white rounded-[2rem] p-6 shadow-xl border-2 border-rose-100">
                <div class="flex justify-between items-center mb-4">
                    <h2 class="text-xl font-bold text-rose-500 flex items-center">
                        <span class="mr-2">📂</span> ประวัติ Test Case ที่บันทึกไว้
                    </h2>
                </div>
                <div id="testCaseHistoryList" class="space-y-2">
                    <!-- Historical test case groups will be rendered here -->
                </div>
                <div id="testCaseHistoryEmpty" class="text-center py-6 text-rose-200">
                    <div class="text-3xl mb-1">📋</div>
                    ยังไม่มีประวัติการบันทึก
                </div>
            </div>
        </div>

        <!-- ==================== PAGE 2: JSON Payload Generator ==================== -->
        <div id="page2" class="hidden">

            <!-- Step 1: Download Template -->
            <div class="cat-card p-8 shadow-2xl mb-8 fade-in">
                <h2 class="text-xl font-bold text-rose-500 mb-4 flex items-center">
                    <span class="text-2xl mr-2">①</span> ดาวน์โหลดไฟล์ตัวอย่าง
                </h2>
                <p class="text-gray-500 mb-4 text-sm">ดาวน์โหลดไฟล์ Excel ตัวอย่างเพื่อใส่ JSON Key-Value ที่ต้องการ
                    แล้วอัปโหลดกลับมา</p>
                <a href="/api/template"
                    class="btn-primary inline-flex items-center px-6 py-3 rounded-full font-bold shadow-lg">
                    📥 ดาวน์โหลดไฟล์ตัวอย่าง (Download Template)
                </a>
            </div>

            <!-- Step 2: Upload -->
            <div class="cat-card p-8 shadow-2xl mb-8 fade-in">
                <h2 class="text-xl font-bold text-rose-500 mb-4 flex items-center">
                    <span class="text-2xl mr-2">②</span> อัปโหลดไฟล์ที่แก้ไขแล้ว
                </h2>
                <div id="uploadZone" class="upload-zone p-10 text-center cursor-pointer">
                    <div class="text-5xl mb-3">📂</div>
                    <p class="text-rose-400 font-medium">คลิกหรือลากไฟล์ .xlsx มาวางที่นี่</p>
                    <p class="text-rose-300 text-sm mt-1">รองรับเฉพาะไฟล์ .xlsx เท่านั้น</p>
                    <input type="file" id="fileInput" accept=".xlsx" class="hidden">
                </div>
                <div id="fileInfo"
                    class="mt-4 hidden bg-green-50 border border-green-200 rounded-xl p-3 flex items-center justify-between">
                    <span class="text-green-600 font-medium text-sm" id="fileName">📄 filename.xlsx</span>
                    <button onclick="clearUpload()" class="text-red-400 hover:text-red-600 text-sm font-medium">✕
                        ลบ</button>
                </div>
            </div>

            <!-- Step 3: Preview Payloads -->
            <div id="payloadSection" class="hidden fade-in">
                <div class="bg-white rounded-[2rem] p-6 shadow-xl border-2 border-rose-100 mb-8">
                    <div class="flex justify-between items-center mb-6">
                        <h2 class="text-xl font-bold text-rose-500 flex items-center">
                            <span class="text-2xl mr-2">③</span> Payload ที่สร้างได้ (<span id="payloadCount">0</span>
                            รายการ)
                        </h2>
                        <div class="flex space-x-2">
                            <button id="copyAllBtn" onclick="copyAllPayloads()"
                                class="bg-blue-50 text-blue-500 px-4 py-2 rounded-xl text-sm font-medium hover:bg-blue-100 transition-colors">
                                📋 คัดลอกทั้งหมด
                            </button>
                            <button id="exportPayloadBtn" onclick="exportPayloads()"
                                class="bg-green-100 text-green-600 px-4 py-2 rounded-xl text-sm font-medium hover:bg-green-200 transition-colors">
                                📥 ส่งออกเป็น Excel
                            </button>
                        </div>
                    </div>

                    <div id="payloadCards" class="space-y-4">
                        <!-- Payload cards will be rendered here -->
                    </div>
                </div>
            </div>

            <!-- Step 4: Upload History -->
            <div class="bg-white rounded-[2rem] p-6 shadow-xl border-2 border-rose-100 mb-8">
                <div class="flex justify-between items-center mb-4">
                    <h2 class="text-xl font-bold text-rose-500 flex items-center">
                        <span class="text-2xl mr-2">④</span> ประวัติการอัปโหลด (History)
                    </h2>
                    <button onclick="clearHistory()"
                        class="bg-rose-50 text-rose-400 px-4 py-2 rounded-xl text-sm font-medium hover:bg-rose-100 transition-colors">
                        🗑️ ล้างประวัติ
                    </button>
                </div>
                <div id="historyList" class="space-y-2">
                    <!-- History items rendered here -->
                </div>
                <div id="historyEmpty" class="text-center py-6 text-rose-200">
                    <div class="text-3xl mb-1">📁</div>
                    ยังไม่มีประวัติการอัปโหลด
                </div>
            </div>
        </div>

        <!-- ==================== PAGE 3: Test Coverage Matrix ==================== -->
        <div id="page3" class="hidden">
            <!-- Configuration / Input Card -->
            <div class="cat-card p-8 shadow-2xl relative mb-10">
                <h2 class="text-xl font-bold text-rose-500 mb-4 flex items-center">
                    <span class="text-2xl mr-2">📊</span> จัดการ Test Scenario และเงื่อนไข
                </h2>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <!-- Test Cases Input -->
                    <div>
                        <label class="block text-sm font-medium text-rose-600 mb-1">Test Scenario</label>
                        <div class="flex gap-2 mb-2">
                            <input type="text" id="matrixTcInput" placeholder="เช่น TC_โอนในประเทศ_Acc1_Bank1" class="flex-1 p-3 bg-rose-50 text-sm">
                            <button onclick="addMatrixTestCase()" class="btn-primary px-4 py-2 rounded-xl text-sm font-bold shadow">เพิ่ม</button>
                        </div>
                        <p class="text-xs text-rose-300">ระบุ Test Scenario เป็นแถว (คุณสามารถพิมพ์เพิ่ม หรือคลิกดึงข้อมูลจาก Test Case หน้าแรกได้)</p>
                    </div>
                    <!-- Requirements/Features Input -->
                    <div>
                        <label class="block text-sm font-medium text-rose-600 mb-1">เงื่อนไข (Column)</label>
                        <div class="flex gap-2 mb-2">
                            <input type="text" id="reqInput" placeholder="เช่น โอนเงินในประเทศ" class="flex-1 p-3 bg-rose-50 text-sm">
                            <button onclick="addRequirement()" class="btn-primary px-4 py-2 rounded-xl text-sm font-bold shadow">เพิ่ม</button>
                        </div>
                        <p class="text-xs text-rose-300">ระบุเงื่อนไขหรือฟังก์ชันที่ต้องการตรวจสอบในคอลัมน์</p>
                    </div>
                </div>
            </div>

            <!-- Preview Matrix Table Card -->
            <div class="bg-white rounded-[2rem] p-6 shadow-xl border-2 border-rose-100 mb-8">
                <div class="flex flex-wrap justify-between items-center gap-3 mb-6">
                    <h2 class="text-xl font-bold text-rose-500 flex items-center">
                        <span class="mr-2">📋</span> ตารางความครอบคลุม (Coverage Matrix Preview)
                    </h2>
                    <div class="flex flex-wrap gap-2">
                        <button onclick="pullTestCasesFromPage1()"
                            class="bg-purple-50 text-purple-500 px-4 py-2 rounded-xl text-sm font-medium hover:bg-purple-100 transition-colors">
                            📥 ดึงจากหน้าแรก
                        </button>
                        <button onclick="requestAIAutoMap()"
                            class="bg-amber-50 text-amber-500 px-4 py-2 rounded-xl text-sm font-medium hover:bg-amber-100 transition-colors">
                            🤖 ให้คุมะจับคู่ให้
                        </button>
                        <button onclick="copyForGoogleSheets()"
                            class="bg-blue-50 text-blue-500 px-4 py-2 rounded-xl text-sm font-medium hover:bg-blue-100 transition-colors"
                            title="คัดลอกเป็นแบบตารางสำหรับวางใน Google Sheets (Ctrl+V) ได้ทันที">
                            🟢 คัดลอกไป Google Sheets
                        </button>
                        <button onclick="exportMatrixExcel()"
                            class="bg-green-100 text-green-600 px-4 py-2 rounded-xl text-sm font-medium hover:bg-green-200 transition-colors">
                            📥 ส่งออก Excel
                        </button>
                        <button onclick="resetMatrix()"
                            class="bg-rose-50 text-rose-400 px-4 py-2 rounded-xl text-sm font-medium hover:bg-rose-100 transition-colors">
                            ล้างทั้งหมด
                        </button>
                    </div>
                </div>

                <!-- Matrix Statistics Summary -->
                <div class="grid grid-cols-3 gap-4 mb-6 bg-rose-50/30 p-4 rounded-2xl border border-rose-100 text-center">
                    <div>
                        <div class="text-xs text-rose-400 font-semibold">เงื่อนไขทั้งหมด</div>
                        <div id="statTotalReqs" class="text-xl font-bold text-rose-600">0</div>
                    </div>
                    <div>
                        <div class="text-xs text-rose-400 font-semibold">Test Scenario ทั้งหมด</div>
                        <div id="statTotalTcs" class="text-xl font-bold text-rose-600">0</div>
                    </div>
                    <div>
                        <div class="text-xs text-rose-400 font-semibold">ความครอบคลุม (Coverage)</div>
                        <div id="statCoveragePct" class="text-xl font-bold text-rose-600">0%</div>
                    </div>
                </div>

                <!-- Interactive Matrix Table -->
                <div class="overflow-x-auto">
                    <table class="w-full text-left border-collapse min-w-[600px]">
                        <thead id="matrixHeader">
                            <!-- Dynamically generated headers -->
                        </thead>
                        <tbody id="matrixBody">
                            <!-- Dynamically generated rows -->
                        </tbody>
                    </table>
                    <div id="matrixEmptyState" class="text-center py-10 text-rose-200">
                        <div class="text-5xl mb-2">📊</div>
                        ยังไม่มี Test Scenario หรือเงื่อนไข เพิ่มข้อมูลด้านบนเพื่อเริ่มจับคู่!
                    </div>
                </div>
            </div>

            <!-- Page 3 History -->
            <div class="mt-8 bg-white rounded-[2rem] p-6 shadow-xl border-2 border-rose-100">
                <div class="flex justify-between items-center mb-4">
                    <h2 class="text-xl font-bold text-rose-500 flex items-center">
                        <span class="mr-2">📂</span> ประวัติ Test Coverage Matrix ที่บันทึกไว้
                    </h2>
                    <button onclick="saveMatrixHistory()"
                        class="bg-blue-100 text-blue-600 px-4 py-2 rounded-xl text-sm font-medium hover:bg-blue-200 transition-colors">
                        💾 บันทึกประวัติ
                    </button>
                </div>
                <div id="matrixHistoryList" class="space-y-2">
                    <!-- Historical matrices will be rendered here -->
                </div>
                <div id="matrixHistoryEmpty" class="text-center py-6 text-rose-200">
                    <div class="text-3xl mb-1">📊</div>
                    ยังไม่มีประวัติการบันทึก
                </div>
            </div>
        </div>

        <!-- ==================== PAGE GISX: GISX Automate (Register Case) ==================== -->
        <div id="pageGisx" class="hidden">
            <!-- Header Card -->
            <div class="cat-card p-8 shadow-2xl relative mb-8 fade-in text-center md:text-left">
                <h2 class="text-2xl font-bold text-rose-500 mb-2 flex items-center justify-center md:justify-start gap-2">
                    <span class="text-3xl">🤖</span> GISX Automate: สร้าง Register Case
                </h2>
                <p class="text-sm text-gray-500">
                    อัปโหลดไฟล์นโยบายประกัน (Excel) เพื่อสร้างเคสในระบบอัตโนมัติ ออกแบบสคริปต์กรอกฟอร์ม และวิเคราะห์ความครบถ้วนของฟิลด์ข้อมูลเมี้ยว 🐾
                </p>
            </div>

            <!-- Download Template & Upload Card Grid -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
                <!-- Download Template -->
                <div class="bg-white rounded-[2rem] p-6 shadow-xl border-2 border-rose-100 flex flex-col justify-between">
                    <div>
                        <h3 class="text-lg font-bold text-rose-500 mb-2 flex items-center gap-2">
                            <span>📥</span> 1. ดาวน์โหลดเทมเพลต Excel
                        </h3>
                        <p class="text-gray-500 text-sm mb-4 leading-relaxed">
                            ดาวน์โหลดไฟล์เทมเพลตสำหรับกรอกเคส โดยคอลัมน์ที่ **บังคับกรอก (Required)** จะแสดงเป็น **สีแดง** และคอลัมน์ที่เป็นตัวเลือกรายการมีดรอปดาวน์เลือกใน Excel ให้เหมือนหน้าจอเว็บ GISX จริงครับเมี้ยว!
                        </p>
                    </div>
                    <button onclick="downloadGisxTemplate()"
                        class="btn-primary w-full py-3 rounded-2xl font-bold shadow-md hover:shadow-lg flex items-center justify-center gap-2">
                        📥 ดาวน์โหลดเทมเพลต (Excel Template)
                    </button>
                </div>

                <!-- Upload File -->
                <div class="bg-white rounded-[2rem] p-6 shadow-xl border-2 border-rose-100">
                    <h3 class="text-lg font-bold text-rose-500 mb-2 flex items-center gap-2">
                        <span>📤</span> 2. อัปโหลดไฟล์ประกันที่กรอกแล้ว
                    </h3>
                    <div id="gisxUploadZone" class="upload-zone p-8 text-center cursor-pointer border-2 border-dashed border-rose-200 rounded-2xl hover:bg-rose-50/20 transition-all flex flex-col items-center justify-center min-h-[140px]">
                        <div class="text-4xl mb-2">📊</div>
                        <p class="text-rose-500 font-bold text-sm">ลากไฟล์ Excel (.xlsx) มาวางที่นี่</p>
                        <p class="text-rose-300 text-xs mt-1">หรือคลิกเพื่อค้นหาและเลือกไฟล์</p>
                        <input type="file" id="gisxFileInput" accept=".xlsx" class="hidden" onchange="handleGisxFileUpload(this)">
                    </div>
                </div>
            </div>

            <!-- Upload File Info (Success notification) -->
            <div id="gisxFileInfo" class="hidden mb-6 bg-green-50 border border-green-200 rounded-2xl p-4 flex items-center justify-between shadow-sm">
                <div class="flex items-center gap-2 text-green-700 font-bold text-sm">
                    <span>🟢</span> ประมวลผลสำเร็จ: <span id="gisxFileName" class="underline">filename.xlsx</span>
                </div>
                <button onclick="clearGisxUpload()" class="text-red-400 hover:text-red-600 font-bold text-sm">✕ ลบตาราง</button>
            </div>

            <!-- Statistics Summary -->
            <div id="gisxStatsSection" class="hidden grid grid-cols-3 gap-4 mb-6 bg-rose-50/30 p-4 rounded-2xl border border-rose-100 text-center shadow-sm">
                <div>
                    <div class="text-xs text-rose-400 font-semibold">จำนวนเคสทั้งหมด</div>
                    <div id="gisxStatTotal" class="text-xl font-bold text-rose-600">0</div>
                </div>
                <div>
                    <div class="text-xs text-rose-400 font-semibold">เคสข้อมูลครบถ้วน</div>
                    <div id="gisxStatValid" class="text-xl font-bold text-green-600">0</div>
                </div>
                <div>
                    <div class="text-xs text-rose-400 font-semibold">เคสต้องกรอกเพิ่ม</div>
                    <div id="gisxStatInvalid" class="text-xl font-bold text-red-500">0</div>
                </div>
            </div>

            <!-- Cases Grid Section -->
            <div id="gisxCasesSection" class="hidden bg-white rounded-[2rem] p-6 shadow-xl border-2 border-rose-100 mb-8">
                <div class="flex flex-wrap justify-between items-center gap-4 mb-6">
                    <h3 class="text-lg font-bold text-rose-500 flex items-center gap-2">
                        <span>📋</span> รายการผู้เอาประกันภัยที่พร้อมสร้าง (Register Cases)
                    </h3>
                    <div class="flex flex-wrap gap-2">
                        <button onclick="copyGisxBatchJson()"
                            class="bg-blue-50 text-blue-500 px-4 py-2 rounded-xl text-sm font-medium hover:bg-blue-100 transition-colors flex items-center gap-1 shadow-sm">
                            📋 คัดลอก JSON Payloads
                        </button>
                        <button onclick="downloadPlaywrightBatch()"
                            class="bg-emerald-50 text-emerald-600 px-4 py-2 rounded-xl text-sm font-medium hover:bg-emerald-100 transition-colors flex items-center gap-1 shadow-sm">
                            🚀 โหลดสคริปต์ Playwright
                        </button>
                        <button onclick="downloadRobotBatch()"
                            class="bg-indigo-50 text-indigo-600 px-4 py-2 rounded-xl text-sm font-medium hover:bg-indigo-100 transition-colors flex items-center gap-1 shadow-sm">
                            🤖 โหลดสคริปต์ Robot Framework
                        </button>
                        <button onclick="clearGisxCases()"
                            class="bg-rose-50 text-rose-400 px-4 py-2 rounded-xl text-sm font-medium hover:bg-rose-100 transition-colors shadow-sm">
                            ล้างทั้งหมด
                        </button>
                    </div>
                </div>

                <!-- Cases Interactive Grid -->
                <div class="overflow-x-auto">
                    <table class="w-full text-left border-collapse min-w-[900px]">
                        <thead>
                            <tr class="border-b-2 border-rose-50 bg-rose-50/20">
                                <th class="py-3 px-3 text-rose-500 font-semibold text-xs text-center w-12">ลำดับ</th>
                                <th class="py-3 px-3 text-rose-500 font-semibold text-xs w-28">เลขใบคำขอ</th>
                                <th class="py-3 px-3 text-rose-500 font-semibold text-xs w-36">ชื่อ-นามสกุล</th>
                                <th class="py-3 px-3 text-rose-500 font-semibold text-xs w-28">ช่องทางขาย</th>
                                <th class="py-3 px-3 text-rose-500 font-semibold text-xs w-32">วันเกิด / เพศ</th>
                                <th class="py-3 px-3 text-rose-500 font-semibold text-xs w-48">แผนประกัน</th>
                                <th class="py-3 px-3 text-rose-500 font-semibold text-xs w-24 text-right">เบี้ยประกัน</th>
                                <th class="py-3 px-3 text-rose-500 font-semibold text-xs text-center w-24">ความสมบูรณ์</th>
                                <th class="py-3 px-3 text-rose-500 font-semibold text-xs text-center w-24">จัดการ</th>
                            </tr>
                        </thead>
                        <tbody id="gisxCasesBody">
                            <!-- Dynamically loaded rows -->
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <!-- Edit Case Modal -->
        <div id="editGisxCaseModal" class="fixed inset-0 bg-black/50 z-[1000] hidden flex items-center justify-center p-4 animate-fade-in">
            <div class="bg-white rounded-3xl p-6 w-full max-w-2xl max-h-[85vh] overflow-y-auto border-4 border-rose-50 shadow-2xl relative">
                <button onclick="closeEditGisxModal()" class="absolute top-4 right-4 text-gray-400 hover:text-rose-500 text-xl font-bold">✕</button>
                <h3 class="text-xl font-bold text-rose-500 mb-4 flex items-center gap-2">
                    <span>✏️</span> แก้ไขข้อมูลผู้เอาประกันภัย (Register Case)
                </h3>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
                    <div>
                        <label class="block text-xs font-semibold text-rose-500 mb-1">เลขที่ใบคำขอ (Required)</label>
                        <input type="text" id="edit_appNo" class="w-full p-2.5 border-2 border-rose-50 rounded-xl focus:outline-none focus:border-rose-300 text-sm">
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-rose-500 mb-1">ช่องทางการขาย (Required)</label>
                        <select id="edit_salesChannel" class="w-full p-2.5 border-2 border-rose-50 rounded-xl focus:outline-none focus:border-rose-300 text-sm bg-white">
                            <option value="Agent">Agent</option>
                            <option value="Bancassurance">Bancassurance</option>
                            <option value="Telesales">Telesales</option>
                            <option value="Direct">Direct</option>
                            <option value="Online">Online</option>
                        </select>
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-rose-500 mb-1">รหัสผู้เสนอขาย (Required)</label>
                        <input type="text" id="edit_agentCode" class="w-full p-2.5 border-2 border-rose-50 rounded-xl focus:outline-none focus:border-rose-300 text-sm">
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-rose-500 mb-1">คำนำหน้านาม</label>
                        <select id="edit_title" class="w-full p-2.5 border-2 border-rose-50 rounded-xl focus:outline-none focus:border-rose-300 text-sm bg-white">
                            <option value="นาย">นาย</option>
                            <option value="นาง">นาง</option>
                            <option value="นางสาว">นางสาว</option>
                            <option value="เด็กชาย">เด็กชาย</option>
                            <option value="เด็กหญิง">เด็กหญิง</option>
                        </select>
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-rose-500 mb-1">ชื่อผู้เอาประกัน (Required)</label>
                        <input type="text" id="edit_firstName" class="w-full p-2.5 border-2 border-rose-50 rounded-xl focus:outline-none focus:border-rose-300 text-sm">
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-rose-500 mb-1">นามสกุลผู้เอาประกัน (Required)</label>
                        <input type="text" id="edit_lastName" class="w-full p-2.5 border-2 border-rose-50 rounded-xl focus:outline-none focus:border-rose-300 text-sm">
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-rose-500 mb-1">เพศ (Required)</label>
                        <select id="edit_gender" class="w-full p-2.5 border-2 border-rose-50 rounded-xl focus:outline-none focus:border-rose-300 text-sm bg-white">
                            <option value="ชาย">ชาย</option>
                            <option value="หญิง">หญิง</option>
                        </select>
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-rose-500 mb-1">วันเกิด YYYY-MM-DD (Required)</label>
                        <input type="text" id="edit_dob" placeholder="1995-12-31" class="w-full p-2.5 border-2 border-rose-50 rounded-xl focus:outline-none focus:border-rose-300 text-sm">
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-rose-500 mb-1">อาชีพ</label>
                        <select id="edit_occupation" class="w-full p-2.5 border-2 border-rose-50 rounded-xl focus:outline-none focus:border-rose-300 text-sm bg-white">
                            <option value="พนักงานบริษัท">พนักงานบริษัท</option>
                            <option value="เจ้าของธุรกิจ">เจ้าของธุรกิจ</option>
                            <option value="ข้าราชการ">ข้าราชการ</option>
                            <option value="แม่บ้าน">แม่บ้าน</option>
                            <option value="นักเรียน/นักศึกษา">นักเรียน/นักศึกษา</option>
                            <option value="ค้าขาย">ค้าขาย</option>
                        </select>
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-rose-500 mb-1">แผนประกันภัย (Required)</label>
                        <select id="edit_plan" class="w-full p-2.5 border-2 border-rose-50 rounded-xl focus:outline-none focus:border-rose-300 text-sm bg-white">
                            <option value="เมืองไทย แฮปปี้ เซฟเวอร์ 11/1">เมืองไทย แฮปปี้ เซฟเวอร์ 11/1</option>
                            <option value="เมืองไทย คุ้มครองตลอดชีพ 99/9">เมืองไทย คุ้มครองตลอดชีพ 99/9</option>
                            <option value="เมืองไทย สมาร์ท ลิงค์ 15/1">เมืองไทย สมาร์ท ลิงค์ 15/1</option>
                            <option value="เมืองไทย เซฟวิ่ง 10/5">เมืองไทย เซฟวิ่ง 10/5</option>
                        </select>
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-rose-500 mb-1">จำนวนเงินเอาประกันภัย (Required)</label>
                        <input type="number" id="edit_sumAssured" class="w-full p-2.5 border-2 border-rose-50 rounded-xl focus:outline-none focus:border-rose-300 text-sm">
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-rose-500 mb-1">งวดการชำระเบี้ย (Required)</label>
                        <select id="edit_mode" class="w-full p-2.5 border-2 border-rose-50 rounded-xl focus:outline-none focus:border-rose-300 text-sm bg-white">
                            <option value="รายปี">รายปี</option>
                            <option value="ราย 6 เดือน">ราย 6 เดือน</option>
                            <option value="ราย 3 เดือน">ราย 3 เดือน</option>
                            <option value="รายเดือน">รายเดือน</option>
                            <option value="ชำระครั้งเดียว">ชำระครั้งเดียว</option>
                        </select>
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-rose-500 mb-1">จำนวนเบี้ยประกันภัย (Required)</label>
                        <input type="number" id="edit_premium" class="w-full p-2.5 border-2 border-rose-50 rounded-xl focus:outline-none focus:border-rose-300 text-sm">
                    </div>
                    <div>
                        <label class="block text-xs font-semibold text-rose-500 mb-1">หมายเหตุ (Remark)</label>
                        <input type="text" id="edit_remark" class="w-full p-2.5 border-2 border-rose-50 rounded-xl focus:outline-none focus:border-rose-300 text-sm">
                    </div>
                </div>
                <div class="flex justify-end gap-2">
                    <button onclick="closeEditGisxModal()" class="px-4 py-2 border border-gray-200 text-gray-500 text-sm font-semibold rounded-xl hover:bg-gray-50">ยกเลิก</button>
                    <button onclick="saveEditGisxCase()" class="btn-primary px-6 py-2 rounded-xl text-sm font-bold shadow-md">บันทึกข้อมูล</button>
                </div>
            </div>
        </div>
    </div>

    <script>
        // ==================== Navigation ====================
        function switchPage(page) {
            const pageHome = document.getElementById('pageHome');
            const page1 = document.getElementById('page1');
            const page2 = document.getElementById('page2');
            const page3 = document.getElementById('page3');
            const pageGisx = document.getElementById('pageGisx');
            const tabHome = document.getElementById('tabPageHome');
            const tab1 = document.getElementById('tabPage1');
            const tab2 = document.getElementById('tabPage2');
            const tab3 = document.getElementById('tabPage3');
            const tabGisx = document.getElementById('tabPageGisxDropdown');

            if (pageHome) pageHome.classList.add('hidden');
            page1.classList.add('hidden');
            page2.classList.add('hidden');
            page3.classList.add('hidden');
            if (pageGisx) pageGisx.classList.add('hidden');
            
            if (tabHome) tabHome.className = 'tab-inactive px-6 py-3 rounded-full font-bold text-sm transition-all';
            tab1.className = 'tab-inactive px-6 py-3 rounded-full font-bold text-sm transition-all';
            tab2.className = 'tab-inactive px-6 py-3 rounded-full font-bold text-sm transition-all';
            tab3.className = 'tab-inactive px-6 py-3 rounded-full font-bold text-sm transition-all';
            if (tabGisx) tabGisx.className = 'tab-inactive px-6 py-3 rounded-full font-bold text-sm transition-all inline-flex items-center gap-1 focus:outline-none';

            if (page === 'home' || page === 0) {
                if (pageHome) pageHome.classList.remove('hidden');
                if (tabHome) tabHome.className = 'tab-active px-6 py-3 rounded-full font-bold text-sm transition-all';
            } else if (page === 1) {
                page1.classList.remove('hidden');
                tab1.className = 'tab-active px-6 py-3 rounded-full font-bold text-sm transition-all';
            } else if (page === 2) {
                page2.classList.remove('hidden');
                tab2.className = 'tab-active px-6 py-3 rounded-full font-bold text-sm transition-all';
            } else if (page === 3) {
                page3.classList.remove('hidden');
                tab3.className = 'tab-active px-6 py-3 rounded-full font-bold text-sm transition-all';
            } else if (page === 'gisx_register_case' || page === 4) {
                if (pageGisx) pageGisx.classList.remove('hidden');
                if (tabGisx) tabGisx.className = 'tab-active px-6 py-3 rounded-full font-bold text-sm transition-all inline-flex items-center gap-1 focus:outline-none';
            }
        }

        window.searchYoutube = async function() {
            const input = document.getElementById('ytSearchInput');
            const resultsDiv = document.getElementById('ytResults');
            if (!input || !resultsDiv) return;
            
            const query = input.value.trim();
            if (!query) {
                alert('กรุณากรอกคำค้นหาก่อนนะแง้ว! 😿');
                return;
            }
            
            resultsDiv.innerHTML = '<div class="col-span-full text-center py-4 text-rose-400">🐾 กำลังค้นหาคลิปบน YouTube ให้รอแป๊บนึงนะเมี้ยว...</div>';
            resultsDiv.classList.remove('hidden');
            
            try {
                const res = await fetch(`/api/youtube-search?q=${encodeURIComponent(query)}`);
                const data = await res.json();
                
                if (!data.items || data.items.length === 0) {
                    resultsDiv.innerHTML = '<div class="col-span-full text-center py-4 text-gray-400">ไม่พบวิดีโอที่ค้นหาครับ ลองเปลี่ยนคำค้นดูนะแง้ว 😿</div>';
                    return;
                }
                
                resultsDiv.innerHTML = '';
                data.items.forEach(video => {
                    const card = document.createElement('div');
                    card.className = 'bg-rose-50/20 border border-rose-100/50 rounded-xl p-2 cursor-pointer hover:bg-rose-50/50 hover:shadow-md transition-all text-left flex flex-col gap-2';
                    card.onclick = () => playYoutubeVideo(video.id, video.title);
                    
                    card.innerHTML = `
                        <div class="relative w-full overflow-hidden rounded-lg" style="padding-top: 56.25%;">
                            <img src="${video.thumbnail}" class="absolute top-0 left-0 w-full h-full object-cover">
                            <span class="absolute bottom-1 right-1 bg-black/75 text-white text-[10px] px-1 rounded font-bold">${video.duration}</span>
                        </div>
                        <div class="flex-1 flex flex-col justify-between">
                            <p class="font-bold text-gray-700 text-xs line-clamp-2 leading-tight" title="${video.title}">${escapeHtml(video.title)}</p>
                            <p class="text-[10px] text-gray-400 mt-1 truncate">${escapeHtml(video.channel)}</p>
                        </div>
                    `;
                    resultsDiv.appendChild(card);
                });
            } catch (err) {
                console.error('YouTube search failed:', err);
                resultsDiv.innerHTML = '<div class="col-span-full text-center py-4 text-red-400">เกิดข้อผิดพลาดในการค้นหา ลองอีกครั้งนะแง้ว! 😿</div>';
            }
        };

        window.playYoutubeVideo = function(videoId, title) {
            const player = document.getElementById('ytPlayer');
            if (player) {
                player.src = `https://www.youtube.com/embed/${videoId}?autoplay=1`;
                showToast(`กำลังเล่น: ${title} 🐾📺`);
            }
        };

        // ==================== PAGE 1: Test Case Builder ====================
        const testCaseNameInput = document.getElementById('testCaseName');
        const testStepInput = document.getElementById('testStep');
        const expectedResultInput = document.getElementById('expectedResult');
        const addStepBtn = document.getElementById('addStepBtn');
        const tableBody = document.getElementById('testCaseTableBody');
        const emptyState = document.getElementById('emptyState');
        const resetBtn = document.getElementById('resetBtn');
        const exportBtn = document.getElementById('exportBtn');

        let testCases = [];

        function validateInputs() {
            const hasName = testCaseNameInput.value.trim() !== '';
            const hasStep = testStepInput.value.trim() !== '';
            const hasExpected = expectedResultInput.value.trim() !== '';
            addStepBtn.disabled = !(hasName && hasStep && hasExpected);
        }

        [testCaseNameInput, testStepInput, expectedResultInput].forEach(input => {
            input.addEventListener('input', validateInputs);
        });

        addStepBtn.addEventListener('click', () => {
            testCases.push({
                name: testCaseNameInput.value.trim(),
                step: testStepInput.value.trim(),
                expected: expectedResultInput.value.trim()
            });
            renderTable();
            testStepInput.value = '';
            expectedResultInput.value = '';
            validateInputs();
            testStepInput.focus();
        });



        function escapeHtml(text) {
            const div = document.createElement('div');
            div.textContent = text;
            return div.innerHTML;
        }

        window.deleteStep = function (index) {
            testCases.splice(index, 1);
            renderTable();
        };

        resetBtn.addEventListener('click', () => {
            if (confirm('คุณต้องการล้างข้อมูลทั้งหมดใช่หรือไม่?')) {
                testCases = [];
                testCaseNameInput.value = '';
                testStepInput.value = '';
                expectedResultInput.value = '';
                renderTable();
                validateInputs();
            }
        });

        // Import Test Cases from Excel
        async function importTestCases(input) {
            const file = input.files[0];
            if (!file) return;

            try {
                const arrayBuffer = await file.arrayBuffer();
                const workbook = new ExcelJS.Workbook();
                await workbook.xlsx.load(arrayBuffer);

                const worksheet = workbook.getWorksheet(1); // Get first sheet
                const newTestCases = [];

                worksheet.eachRow((row, rowNumber) => {
                    if (rowNumber === 1) return; // Skip header

                    const name = row.getCell(2).value;
                    const step = row.getCell(3).value;
                    const expected = row.getCell(4).value;

                    if (name || step || expected) {
                        newTestCases.push({
                            name: name?.toString() || '',
                            step: step?.toString() || '',
                            expected: expected?.toString() || ''
                        });
                    }
                });

                if (newTestCases.length > 0) {
                    if (testCases.length > 0 && confirm('ต้องการล้างข้อมูลเดิมและแทนที่ด้วยข้อมูลใหม่ใช่หรือไม่?\n(ยกเลิกเพื่อเพิ่มต่อท้าย)')) {
                        testCases = newTestCases;
                    } else {
                        testCases = [...testCases, ...newTestCases];
                    }
                    renderTable();
                    showToast('นำเข้าข้อมูลสำเร็จ! 🐱');
                } else {
                    alert('ไม่พบข้อมูลที่ถูกต้องในไฟล์ Excel');
                }
            } catch (error) {
                console.error('Import failed:', error);
                alert('เกิดข้อผิดพลาดในการนำเข้าไฟล์: ' + error.message);
            } finally {
                input.value = ''; // Clear input for next time
            }
        }

        // Page 1 Excel Export (ExcelJS)
        exportBtn.addEventListener('click', async () => {
            try {
                const workbook = new ExcelJS.Workbook();
                const worksheet = workbook.addWorksheet('Test Cases');
                worksheet.columns = [
                    { header: 'ลำดับ', key: 'no', width: 10 },
                    { header: 'ชื่อกรณีทดสอบ', key: 'name', width: 30 },
                    { header: 'ขั้นตอนการทดสอบ', key: 'step', width: 50 },
                    { header: 'ผลลัพธ์ที่คาดหวัง', key: 'expected', width: 50 }
                ];
                worksheet.getRow(1).font = { bold: true };
                worksheet.getRow(1).alignment = { vertical: 'middle' };
                testCases.forEach((tc, index) => {
                    const row = worksheet.addRow({ no: index + 1, name: tc.name, step: tc.step, expected: tc.expected });
                    row.getCell('step').alignment = { wrapText: true, vertical: 'top' };
                    row.getCell('expected').alignment = { wrapText: true, vertical: 'top' };
                    row.getCell('name').alignment = { vertical: 'top' };
                    row.getCell('no').alignment = { vertical: 'top' };
                });
                const rawName = testCases[0].name || 'test-cases';
                const safeName = rawName.replace(/[\\\/:*?"<>|]/g, '_').trim();
                const buffer = await workbook.xlsx.writeBuffer();
                const blob = new Blob([buffer], { type: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet' });
                saveAs(blob, `Cat_Test_Case_${safeName || 'Export'}.xlsx`);
            } catch (error) {
                console.error('Export failed:', error);
                alert('เกิดข้อผิดพลาดในการส่งออกไฟล์: ' + error.message);
            }
        });

        window.copyTestCasesForGoogleSheets = function () {
            if (testCases.length === 0) {
                alert('ยังไม่มีข้อมูล Test Case ในการคัดลอกครับ 😿');
                return;
            }
            
            let tsv = '';
            
            // Header Row
            const headers = ['ลำดับ', 'ชื่อกรณีทดสอบ', 'ขั้นตอนการทดสอบ', 'ผลลัพธ์ที่คาดหวัง'];
            tsv += headers.join('\t') + '\n';
            
            // Data Rows
            testCases.forEach((tc, idx) => {
                const escapeTsv = (val) => {
                    if (!val) return '';
                    let str = val.toString();
                    if (str.includes('\n') || str.includes('\t') || str.includes('"')) {
                        return '"' + str.replace(/"/g, '""') + '"';
                    }
                    return str;
                };

                const row = [
                    idx + 1,
                    escapeTsv(tc.name),
                    escapeTsv(tc.step),
                    escapeTsv(tc.expected)
                ];
                tsv += row.join('\t') + '\n';
            });
            
            navigator.clipboard.writeText(tsv).then(() => {
                showToast('คัดลอก Test Case ในรูปแบบ Google Sheets สำเร็จ! วาง (Ctrl+V) ได้เลยครับ 🟢🐾');
            }).catch(err => {
                console.error('TSV copy failed:', err);
                alert('ไม่สามารถคัดลอกได้: ' + err.message);
            });
        };

        // ==================== PAGE 2: JSON Payload Generator ====================
        const uploadZone = document.getElementById('uploadZone');
        const fileInput = document.getElementById('fileInput');
        const fileInfo = document.getElementById('fileInfo');
        const fileNameEl = document.getElementById('fileName');
        const payloadSection = document.getElementById('payloadSection');
        const payloadCards = document.getElementById('payloadCards');
        const payloadCountEl = document.getElementById('payloadCount');

        let generatedPayloads = [];

        // Upload zone click
        uploadZone.addEventListener('click', () => fileInput.click());

        // Drag & drop
        uploadZone.addEventListener('dragover', (e) => {
            e.preventDefault();
            uploadZone.classList.add('drag-over');
        });
        uploadZone.addEventListener('dragleave', () => {
            uploadZone.classList.remove('drag-over');
        });
        uploadZone.addEventListener('drop', (e) => {
            e.preventDefault();
            uploadZone.classList.remove('drag-over');
            const file = e.dataTransfer.files[0];
            if (file && file.name.endsWith('.xlsx')) {
                processFile(file);
            } else {
                alert('กรุณาอัปโหลดไฟล์ .xlsx เท่านั้น');
            }
        });

        fileInput.addEventListener('change', (e) => {
            if (e.target.files[0]) processFile(e.target.files[0]);
        });

        async function processFile(file) {
            fileNameEl.textContent = '📄 ' + file.name;
            fileInfo.classList.remove('hidden');

            try {
                const arrayBuffer = await file.arrayBuffer();
                const workbook = new ExcelJS.Workbook();
                await workbook.xlsx.load(arrayBuffer);

                // ---- Nested JSON helper functions ----
                function parsePath(path) {
                    const parts = [];
                    const tokens = path.replace(/\[(\d+)\]/g, '.$1').split('.');
                    tokens.forEach(t => {
                        if (/^\d+$/.test(t)) {
                            parts.push(parseInt(t, 10));
                        } else {
                            parts.push(t);
                        }
                    });
                    return parts;
                }

                function setNestedValue(obj, path, value) {
                    const parts = parsePath(path);
                    let current = obj;
                    for (let i = 0; i < parts.length - 1; i++) {
                        const key = parts[i];
                        const nextKey = parts[i + 1];
                        if (current[key] === undefined) {
                            current[key] = typeof nextKey === 'number' ? [] : {};
                        }
                        current = current[key];
                    }
                    current[parts[parts.length - 1]] = value;
                }

                function parseValue(val) {
                    if (val === 'true') return true;
                    if (val === 'false') return false;
                    if (val === 'null') return null;
                    if (/^-?\d+(\.\d+)?$/.test(val)) return Number(val);
                    return val;
                }

                // ---- Loop through all sheets (skip instruction sheet) ----
                generatedPayloads = [];
                const skippedNames = ['คำแนะนำ', 'instructions'];

                workbook.worksheets.forEach(worksheet => {
                    const sheetName = worksheet.name.toLowerCase().trim();
                    if (skippedNames.some(s => sheetName === s.toLowerCase())) return;

                    const rows = [];
                    worksheet.eachRow((row, rowNumber) => {
                        if (rowNumber === 1) return; // Skip header
                        const key = row.getCell(2).value; // Column B = Key
                        let value = row.getCell(3).value;   // Column C = Value

                        // Handle rich text / formula objects
                        if (value && typeof value === 'object' && value.richText) {
                            value = value.richText.map(rt => rt.text).join('');
                        }
                        if (value && typeof value === 'object' && value.result !== undefined) {
                            value = value.result;
                        }

                        if (key && key.toString().trim()) {
                            rows.push({
                                key: key.toString().trim(),
                                value: value !== null && value !== undefined ? value.toString().trim() : ''
                            });
                        }
                    });

                    if (rows.length === 0) return;

                    // Build nested payload for this sheet
                    const payload = {};
                    rows.forEach(r => {
                        setNestedValue(payload, r.key, parseValue(r.value));
                    });

                    generatedPayloads.push({
                        sheetName: worksheet.name,
                        items: rows,
                        payload
                    });
                });

                if (generatedPayloads.length === 0) {
                    alert('ไม่พบข้อมูล Key-Value ในไฟล์');
                    return;
                }

                renderPayloads();
                saveToHistory(file.name, generatedPayloads);
            } catch (error) {
                console.error('File processing error:', error);
                alert('เกิดข้อผิดพลาดในการอ่านไฟล์: ' + error.message);
            }
        }

        // ==================== Upload History (localStorage) ====================
        const HISTORY_KEY = 'cat_payload_history';

        function getHistory() {
            try {
                return JSON.parse(localStorage.getItem(HISTORY_KEY) || '[]');
            } catch { return []; }
        }

        function saveToHistory(fileName, payloads) {
            const history = getHistory();
            history.unshift({
                id: Date.now(),
                fileName,
                date: new Date().toLocaleString('th-TH'),
                payloadCount: payloads.length,
                payloads: payloads
            });
            // Keep max 20 entries
            if (history.length > 20) history.length = 20;
            localStorage.setItem(HISTORY_KEY, JSON.stringify(history));
            renderHistory();
        }

        function renderHistory() {
            const historyList = document.getElementById('historyList');
            const historyEmpty = document.getElementById('historyEmpty');
            const history = getHistory();

            historyList.innerHTML = '';
            if (history.length === 0) {
                historyEmpty.classList.remove('hidden');
                return;
            }
            historyEmpty.classList.add('hidden');

            history.forEach((item, idx) => {
                const el = document.createElement('div');
                el.className = 'flex items-center justify-between bg-rose-50/50 rounded-xl px-4 py-3 hover:bg-rose-50 transition-colors';
                el.innerHTML = `
                    <div class="flex items-center gap-3">
                        <span class="text-lg">📄</span>
                        <div>
                            <p class="font-medium text-gray-700 text-sm">${escapeHtml(item.fileName)}</p>
                            <p class="text-xs text-gray-400">${item.date} · ${item.payloadCount} payload(s)</p>
                        </div>
                    </div>
                    <div class="flex gap-2">
                        <button onclick="loadFromHistory(${idx})" class="bg-white text-blue-500 px-3 py-1 rounded-lg text-xs font-medium hover:bg-blue-50 transition-colors border border-blue-200">
                            👁️ ดู
                        </button>
                        <button onclick="deleteFromHistory(${idx})" class="bg-white text-red-400 px-3 py-1 rounded-lg text-xs font-medium hover:bg-red-50 transition-colors border border-red-200">
                            ✕
                        </button>
                    </div>
                `;
                historyList.appendChild(el);
            });
        }

        window.loadFromHistory = function (idx) {
            const history = getHistory();
            if (!history[idx]) return;
            generatedPayloads = history[idx].payloads;
            fileNameEl.textContent = '📄 ' + history[idx].fileName + ' (จากประวัติ)';
            fileInfo.classList.remove('hidden');
            renderPayloads();
            showToast('โหลดจากประวัติสำเร็จ! 📂');
        };

        window.deleteFromHistory = function (idx) {
            const history = getHistory();
            history.splice(idx, 1);
            localStorage.setItem(HISTORY_KEY, JSON.stringify(history));
            renderHistory();
            showToast('ลบรายการจากประวัติแล้ว');
        };

        function clearHistory() {
            if (!confirm('ต้องการล้างประวัติทั้งหมดใช่หรือไม่?')) return;
            localStorage.removeItem(HISTORY_KEY);
            renderHistory();
            showToast('ล้างประวัติเรียบร้อย');
        }

        // Render history on page load
        renderHistory();

        // ---- Cat Atmosphere Generator ----
        function createCatAtmosphere() {
            const atmosphere = document.getElementById('cat-atmosphere');
            const catIcons = ['🐱', '🐈', '😺', '🐾', '🐈‍⬛', '🐾', '😸', '😻', '🐾'];
            const count = 40; // Number of cats

            for (let i = 0; i < count; i++) {
                const cat = document.createElement('div');
                cat.className = 'bg-cat';
                cat.textContent = catIcons[Math.floor(Math.random() * catIcons.length)];

                // Random position
                cat.style.left = Math.random() * 100 + '%';
                cat.style.top = Math.random() * 100 + '%';

                // Random size
                const size = 20 + Math.random() * 60;
                cat.style.fontSize = size + 'px';

                // Random animation delay and duration
                cat.style.animationDelay = (Math.random() * -20) + 's';
                cat.style.animationDuration = (15 + Math.random() * 15) + 's';

                atmosphere.appendChild(cat);
            }
        }
        createCatAtmosphere();

        function clearUpload() {
            fileInput.value = '';
            fileInfo.classList.add('hidden');
            payloadSection.classList.add('hidden');
            generatedPayloads = [];
        }

        function renderPayloads() {
            payloadCards.innerHTML = '';
            payloadCountEl.textContent = generatedPayloads.length;
            payloadSection.classList.remove('hidden');

            // ---- Combined JSON block (when 2+ payloads) ----
            if (generatedPayloads.length > 1) {
                const allPayloads = generatedPayloads.map(e => e.payload);
                const combinedJson = JSON.stringify(allPayloads, null, 2);
                const combinedHighlighted = syntaxHighlight(combinedJson);

                const combinedCard = document.createElement('div');
                combinedCard.className = 'border-2 border-indigo-200 rounded-2xl overflow-hidden fade-in mb-6';
                combinedCard.innerHTML = `
                    <div class="bg-indigo-50 px-5 py-3 flex justify-between items-center">
                        <span class="font-bold text-indigo-500">📦 JSON รวมทั้งหมด (${generatedPayloads.length} Payloads)</span>
                        <button onclick="copyCombinedPayloads()" class="bg-white text-indigo-400 px-3 py-1 rounded-lg text-xs font-medium hover:bg-indigo-100 transition-colors border border-indigo-200">
                            📋 คัดลอก JSON รวม
                        </button>
                    </div>
                    <div class="px-5 py-4">
                        <pre class="json-block p-4" style="max-height: 400px; overflow-y: auto;">${combinedHighlighted}</pre>
                    </div>
                `;
                payloadCards.appendChild(combinedCard);
            }

            generatedPayloads.forEach((entry, idx) => {
                const jsonStr = JSON.stringify(entry.payload, null, 2);
                const highlighted = syntaxHighlight(jsonStr);

                const card = document.createElement('div');
                card.className = 'border-2 border-rose-100 rounded-2xl overflow-hidden fade-in';
                card.innerHTML = `
                    <div class="bg-rose-50 px-5 py-3 flex justify-between items-center">
                        <span class="font-bold text-rose-500">🐱 Payload #${idx + 1} ${entry.sheetName ? `<span class="text-rose-300 font-normal text-sm ml-2">(${escapeHtml(entry.sheetName)})</span>` : ''}</span>
                        <button onclick="copyPayload(${idx})" class="bg-white text-rose-400 px-3 py-1 rounded-lg text-xs font-medium hover:bg-rose-100 transition-colors border border-rose-200">
                            📋 คัดลอก
                        </button>
                    </div>
                    <!-- Key-Value Table -->
                    <div class="px-5 py-3">
                        <table class="w-full text-sm mb-3">
                            <thead>
                                <tr class="border-b border-rose-50">
                                    <th class="py-2 px-3 text-left text-rose-400 font-semibold w-12">No.</th>
                                    <th class="py-2 px-3 text-left text-rose-400 font-semibold">Key</th>
                                    <th class="py-2 px-3 text-left text-rose-400 font-semibold">Value</th>
                                </tr>
                            </thead>
                            <tbody class="text-gray-600">
                                ${entry.items.map((item, i) => `
                                    <tr class="border-b border-rose-50/50">
                                        <td class="py-2 px-3">${i + 1}</td>
                                        <td class="py-2 px-3 font-mono text-blue-600">${escapeHtml(item.key)}</td>
                                        <td class="py-2 px-3 font-mono">${escapeHtml(item.value)}</td>
                                    </tr>
                                `).join('')}
                            </tbody>
                        </table>
                    </div>
                    <!-- JSON Preview -->
                    <div class="px-5 pb-4">
                        <p class="text-xs text-gray-400 mb-2 font-medium">JSON Payload:</p>
                        <pre class="json-block p-4">${highlighted}</pre>
                    </div>
                `;
                payloadCards.appendChild(card);
            });
        }

        function syntaxHighlight(json) {
            return json.replace(/("(\\u[a-zA-Z0-9]{4}|\\[^u]|[^\\"])*"(\s*:)?|\b(true|false|null)\b|-?\d+(?:\.\d*)?(?:[eE][+\-]?\d+)?)/g, (match) => {
                let cls = 'json-number';
                if (/^"/.test(match)) {
                    if (/:$/.test(match)) {
                        cls = 'json-key';
                        match = match.replace(/:$/, '') + ':';
                        return `<span class="${cls}">${match.slice(0, -1)}</span>:`;
                    }
                    cls = 'json-string';
                } else if (/true|false/.test(match)) {
                    cls = 'json-bool';
                }
                return `<span class="${cls}">${match}</span>`;
            });
        }

        window.copyPayload = function (idx) {
            const text = JSON.stringify(generatedPayloads[idx].payload, null, 2);
            navigator.clipboard.writeText(text).then(() => {
                showToast('คัดลอก Payload สำเร็จ! 🐱');
            });
        };

        window.copyCombinedPayloads = function () {
            const allPayloads = generatedPayloads.map(e => e.payload);
            const text = JSON.stringify(allPayloads, null, 2);
            navigator.clipboard.writeText(text).then(() => {
                showToast('คัดลอก JSON รวมทั้งหมดสำเร็จ! 📦');
            });
        };

        function copyAllPayloads() {
            const allText = generatedPayloads.map((entry, idx) =>
                `// Payload #${idx + 1}${entry.sheetName ? ' (' + entry.sheetName + ')' : ''}\n${JSON.stringify(entry.payload, null, 2)}`
            ).join('\n\n');
            navigator.clipboard.writeText(allText).then(() => {
                showToast('คัดลอกทั้งหมดสำเร็จ! 🐱');
            });
        }

        async function exportPayloads() {
            try {
                const workbook = new ExcelJS.Workbook();

                generatedPayloads.forEach((entry, pIdx) => {
                    const sheetName = entry.sheetName || `Payload ${pIdx + 1}`;
                    const worksheet = workbook.addWorksheet(sheetName);

                    worksheet.columns = [
                        { header: 'ลำดับ', key: 'no', width: 10 },
                        { header: 'Key', key: 'key', width: 30 },
                        { header: 'Value', key: 'value', width: 40 },
                        { header: 'JSON Payload', key: 'payload', width: 60 }
                    ];

                    worksheet.getRow(1).font = { bold: true, color: { argb: 'FFFFFFFF' } };
                    worksheet.getRow(1).alignment = { vertical: 'middle', horizontal: 'center' };
                    worksheet.getRow(1).fill = { type: 'pattern', pattern: 'solid', fgColor: { argb: 'FFFDA4AF' } };

                    const jsonStr = JSON.stringify(entry.payload, null, 2);
                    entry.items.forEach((item, i) => {
                        const row = worksheet.addRow({
                            no: i + 1,
                            key: item.key,
                            value: item.value,
                            payload: i === 0 ? jsonStr : ''
                        });
                        row.getCell('payload').alignment = { wrapText: true, vertical: 'top' };
                        row.getCell('key').alignment = { vertical: 'top' };
                        row.getCell('value').alignment = { vertical: 'top' };
                        row.getCell('no').alignment = { vertical: 'top' };
                    });
                });

                const buffer = await workbook.xlsx.writeBuffer();
                const blob = new Blob([buffer], { type: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet' });
                saveAs(blob, 'Cat_JSON_Payloads.xlsx');
                showToast('ส่งออก Excel สำเร็จ! 📥');
            } catch (error) {
                console.error('Export failed:', error);
                alert('เกิดข้อผิดพลาดในการส่งออกไฟล์: ' + error.message);
            }
        }

        // ==================== AI SIDEBAR LOGIC ====================
        let isAIOpen = false;
        let chatHistory = [];

        window.toggleAI = function () {
            const sidebar = document.getElementById('ai-sidebar');
            const icon = document.getElementById('ai-toggle-icon');
            isAIOpen = !isAIOpen;
            sidebar.style.transform = isAIOpen ? 'translateX(0)' : 'translateX(-100%)';
            icon.textContent = isAIOpen ? '✕' : '🐱';
        };

        let selectedAIFiles = [];

        window.handleAIFileSelect = function (input) {
            const files = Array.from(input.files);
            selectedAIFiles = [...selectedAIFiles, ...files];
            renderAIFilePreviews();
            input.value = '';
        };

        function renderAIFilePreviews() {
            const container = document.getElementById('ai-file-preview');
            if (selectedAIFiles.length === 0) {
                container.classList.add('hidden');
                return;
            }
            container.classList.remove('hidden');
            container.innerHTML = '';
            selectedAIFiles.forEach((file, index) => {
                const item = document.createElement('div');
                item.className = 'file-preview-item';
                item.innerHTML = `
                    <span class="truncate max-w-[150px]">📄 ${file.name}</span>
                    <button onclick="removeAIFile(${index})" class="hover:text-red-600 font-bold">✕</button>
                `;
                container.appendChild(item);
            });
        }

        window.removeAIFile = function (index) {
            selectedAIFiles.splice(index, 1);
            renderAIFilePreviews();
        };

        // Initialize drag & drop for AI sidebar
        setTimeout(() => {
            const chatContainer = document.getElementById('chat-container');
            if (!chatContainer) return;
            chatContainer.addEventListener('dragover', (e) => {
                e.preventDefault();
                chatContainer.classList.add('drag-over');
            });
            ['dragleave', 'drop'].forEach(evt => {
                chatContainer.addEventListener(evt, () => chatContainer.classList.remove('drag-over'));
            });
            chatContainer.addEventListener('drop', (e) => {
                e.preventDefault();
                const files = Array.from(e.dataTransfer.files);
                selectedAIFiles = [...selectedAIFiles, ...files];
                renderAIFilePreviews();
                showToast('เพิ่มไฟล์แล้วแง้ว! 🐾');
            });
        }, 1000);

        window.sendMessage = async function () {
            const input = document.getElementById('chat-input');
            const msg = input.value.trim();
            if (!msg && selectedAIFiles.length === 0) return;

            // Show what's being sent
            let displayMsg = msg;
            if (selectedAIFiles.length > 0) {
                displayMsg = (msg ? msg + '\n' : '') + selectedAIFiles.map(f => '📎 ' + f.name).join('\n');
            }
            addChatMessage('user', displayMsg);
            input.value = '';
            input.style.height = 'auto';

            const formData = new FormData();
            formData.append('message', msg || "ช่วยวิเคราะห์ไฟล์นี้หน่อยครับ");
            formData.append('history', JSON.stringify(chatHistory));
            selectedAIFiles.forEach(file => formData.append('files', file));

            // Clear preview
            selectedAIFiles = [];
            renderAIFilePreviews();

            // Show typing indicator
            const typingId = addChatMessage('ai', '<span class="typing-dot"></span><span class="typing-dot" style="animation-delay: 0.2s"></span><span class="typing-dot" style="animation-delay: 0.4s"></span>', true);

            try {
                const res = await fetch('/api/chat', {
                    method: 'POST',
                    body: formData // No Content-Type header needed for FormData
                });
                const data = await res.json();

                // Remove typing indicator
                document.getElementById(typingId).remove();

                if (data.error) {
                    addChatMessage('ai', '❌ ' + data.error);
                } else {
                    const executeAction = (actionObj) => {
                        if (!actionObj || !actionObj.action) return;
                        switch (actionObj.action) {
                            case 'add_testcases':
                                if (actionObj.data) {
                                    testCases = [...testCases, ...actionObj.data];
                                    renderTable();
                                    showToast('คุมะเพิ่ม Test Case ให้แล้วครับ! 🐾');
                                }
                                break;
                            case 'delete_testcases':
                                if (actionObj.data && Array.isArray(actionObj.data)) {
                                    // Sort indices descending to avoid shifting issues
                                    const sortedIndices = [...actionObj.data]
                                        .map(i => parseInt(i) - 1)
                                        .filter(i => i >= 0 && i < testCases.length)
                                        .sort((a, b) => b - a);

                                    sortedIndices.forEach(idx => {
                                        testCases.splice(idx, 1);
                                    });
                                    renderTable();
                                    showToast(`คุมะลบข้อที่เลือกให้แล้วครับ ${actionObj.data.join(', ')}! 🐾`);
                                }
                                break;
                            case 'edit_testcases':
                                if (actionObj.data && Array.isArray(actionObj.data)) {
                                    let editCount = 0;
                                    actionObj.data.forEach(edit => {
                                        const idx = parseInt(edit.index) - 1; // 1-based → 0-based
                                        if (idx >= 0 && idx < testCases.length) {
                                            if (edit.name !== undefined) testCases[idx].name = edit.name;
                                            if (edit.step !== undefined) testCases[idx].step = edit.step;
                                            if (edit.expected !== undefined) testCases[idx].expected = edit.expected;
                                            editCount++;
                                        }
                                    });
                                    renderTable();
                                    showToast(`คุมะแก้ไข Test Case ${editCount} รายการให้แล้วครับ! ✏️🐾`);
                                }
                                break;
                            case 'switch_page':
                                if (actionObj.page) {
                                    switchPage(actionObj.page);
                                    showToast(`คุมะเปลี่ยนหน้าไปหน้า ${actionObj.page} ให้แล้วครับ! 🐻`);
                                }
                                break;
                            case 'export_excel':
                                const pageToExport = document.getElementById('page1').classList.contains('hidden') ? (document.getElementById('page2').classList.contains('hidden') ? 3 : 2) : 1;
                                if (pageToExport === 1) {
                                    if (testCases.length > 0) document.getElementById('exportBtn').click();
                                    else showToast('ยังไม่มีข้อมูล Test Case ให้ส่งออกครับ 😿', 'error');
                                } else if (pageToExport === 2) {
                                    if (generatedPayloads.length > 0) document.getElementById('exportPayloadBtn').click();
                                    else showToast('ยังไม่มีข้อมูล Payload ให้ส่งออกครับ 😿', 'error');
                                } else {
                                    exportMatrixExcel();
                                }
                                break;
                            case 'save_history':
                                const activeSavePage = document.getElementById('page1').classList.contains('hidden') ? (document.getElementById('page2').classList.contains('hidden') ? 3 : 2) : 1;
                                if (activeSavePage === 1) {
                                    saveTestCaseHistory();
                                } else if (activeSavePage === 2) {
                                    showToast('หน้านี้บันทึกประวัติอัติโนมัติเมื่ออัปโหลดครับเมี้ยว 🐾');
                                } else {
                                    saveMatrixHistory();
                                }
                                break;
                            case 'clear_all':
                                const activeClearPage = document.getElementById('page1').classList.contains('hidden') ? (document.getElementById('page2').classList.contains('hidden') ? 3 : 2) : 1;
                                if (activeClearPage === 1) {
                                    testCases = []; renderTable();
                                } else if (activeClearPage === 2) {
                                    generatedPayloads = []; renderPayloads();
                                } else {
                                    resetMatrix(true);
                                }
                                showToast('คุมะล้างข้อมูลให้เกลี้ยงเลยครับ! 🧼');
                                break;
                            case 'add_payloads':
                                if (actionObj.data) {
                                    const payloads = Array.isArray(actionObj.data) ? actionObj.data : [actionObj.data];
                                    payloads.forEach(p => {
                                        const entry = {
                                            sheetName: p.sheetName || 'AI Generated',
                                            items: p.items || [],
                                            payload: {}
                                        };
                                        function parsePath(path) {
                                            const parts = [];
                                            const tokens = path.replace(/\[(\d+)\]/g, '.$1').split('.');
                                            tokens.forEach(t => { if (/^\d+$/.test(t)) parts.push(parseInt(t, 10)); else parts.push(t); });
                                            return parts;
                                        }
                                        function setNestedValue(obj, path, value) {
                                            const parts = parsePath(path);
                                            let current = obj;
                                            for (let i = 0; i < parts.length - 1; i++) {
                                                const key = parts[i];
                                                if (current[key] === undefined) current[key] = typeof parts[i + 1] === 'number' ? [] : {};
                                                current = current[key];
                                            }
                                            current[parts[parts.length - 1]] = value;
                                        }
                                        entry.items.forEach(item => setNestedValue(entry.payload, item.key, item.value));
                                        generatedPayloads.push(entry);
                                    });
                                    renderPayloads();
                                    saveToHistory('AI_Generated_Payloads', generatedPayloads);
                                    showToast('คุมะสร้าง JSON Payload ให้แล้วครับ! 🔧');
                                }
                                break;
                            case 'copy_text':
                                if (actionObj.data) {
                                    navigator.clipboard.writeText(actionObj.data).then(() => {
                                        showToast('คุมะคัดลอกข้อความเป็น Clipboard ให้แล้วครับ! 📋');
                                    });
                                }
                                break;
                            case 'load_history':
                                if (typeof actionObj.data === 'number') {
                                    const currentPage = document.getElementById('page1').classList.contains('hidden') ? (document.getElementById('page2').classList.contains('hidden') ? 3 : 2) : 1;
                                    if (currentPage === 1) {
                                        const history = JSON.parse(localStorage.getItem(TESTCASE_HISTORY_KEY) || '[]');
                                        if (history[actionObj.data]) {
                                            testCases = [...history[actionObj.data].data];
                                            renderTable();
                                            showToast('คุมะดึงประวัติ Test Case มาให้แล้วครับ! 📂');
                                        }
                                    } else if (currentPage === 2) {
                                        const history = JSON.parse(localStorage.getItem(HISTORY_KEY) || '[]');
                                        if (history[actionObj.data]) {
                                            generatedPayloads = [...history[actionObj.data].payloads];
                                            renderPayloads();
                                            showToast('คุมะดึงประวัติ Payload มาให้แล้วครับ! 📂');
                                        }
                                    } else {
                                        const history = JSON.parse(localStorage.getItem(MATRIX_HISTORY_KEY) || '[]');
                                        if (history[actionObj.data]) {
                                            matrixRequirements = [...history[actionObj.data].requirements];
                                            matrixTestCases = [...history[actionObj.data].testcases];
                                            matrixMapping = JSON.parse(JSON.stringify(history[actionObj.data].mapping));
                                            renderMatrix();
                                            showToast('คุมะดึงประวัติ Matrix มาให้แล้วครับ! 📂');
                                        }
                                    }
                                }
                                break;
                            case 'update_matrix':
                                if (actionObj.data) {
                                    matrixRequirements = actionObj.data.requirements || [];
                                    matrixTestCases = actionObj.data.testcases || [];
                                    matrixMapping = actionObj.data.mapping || {};
                                    switchPage(3);
                                    renderMatrix();
                                    showToast('คุมะสร้าง Test Coverage Matrix ให้แล้วครับ! 📊🐾');
                                }
                                break;
                        }
                    };

                    if (data.actions && Array.isArray(data.actions)) {
                        data.actions.forEach(act => {
                            executeAction(act);
                        });
                    } else if (data.action) {
                        executeAction(data);
                    }
                    if (data.message) addChatMessage('ai', data.message);

                    // Update internal chat history
                    chatHistory.push({ role: 'user', parts: [{ text: msg }] });
                    chatHistory.push({ role: 'model', parts: [{ text: data.message || '' }] });
                }
            } catch (err) {
                console.error('Chat error detail:', err);
                document.getElementById(typingId).remove();
                addChatMessage('ai', 'ขออภัยครับแม่มนุษย์ คุมะมีปัญหาขัดข้อง... 😿 (รายละเอียด: ' + err.message + ')');
            }
        };

        function addChatMessage(role, text, isRaw = false) {
            const container = document.getElementById('chat-container');
            const id = 'chat-' + Date.now();
            const div = document.createElement('div');
            div.id = id;
            div.className = `chat-bubble ${role} shadow-sm fade-in`;
            if (isRaw) div.innerHTML = text;
            else div.textContent = text;
            container.appendChild(div);
            container.scrollTop = container.scrollHeight;
            return id;
        }

        const chatInput = document.getElementById('chat-input');
        if (chatInput) {
            chatInput.addEventListener('input', function () {
                this.style.height = 'auto';
                this.style.height = (this.scrollHeight) + 'px';
            });

            chatInput.addEventListener('keydown', (e) => {
                if (e.key === 'Enter' && !e.shiftKey) {
                    e.preventDefault();
                    sendMessage();
                }
            });
        }

        // ==================== INLINE EDITING LOGIC ====================
        window.updateTestCaseData = function (index, field, value) {
            testCases[index][field] = value.trim();
        };

        // Modify renderTable to add contenteditable
        function renderTable() {
            tableBody.innerHTML = '';
            const copySheetsBtn = document.getElementById('copySheetsBtn');
            if (testCases.length === 0) {
                emptyState.classList.remove('hidden');
                exportBtn.classList.add('hidden');
                if (copySheetsBtn) copySheetsBtn.classList.add('hidden');
            } else {
                emptyState.classList.add('hidden');
                exportBtn.classList.remove('hidden');
                if (copySheetsBtn) copySheetsBtn.classList.remove('hidden');
                testCases.forEach((tc, index) => {
                    const row = document.createElement('tr');
                    row.className = 'border-b border-rose-50 hover:bg-rose-50/30 transition-colors';
                    row.innerHTML = `
                        <td class="py-4 px-4 font-medium">${index + 1}</td>
                        <td class="py-4 px-4" contenteditable="true" onblur="updateTestCaseData(${index}, 'name', this.innerText)">${escapeHtml(tc.name)}</td>
                        <td class="py-4 px-4 whitespace-pre-line" contenteditable="true" onblur="updateTestCaseData(${index}, 'step', this.innerText)">${escapeHtml(tc.step)}</td>
                        <td class="py-4 px-4 whitespace-pre-line" contenteditable="true" onblur="updateTestCaseData(${index}, 'expected', this.innerText)">${escapeHtml(tc.expected)}</td>
                        <td class="py-4 px-4 text-center">
                            <button onclick="deleteStep(${index})" class="btn-danger w-8 h-8 rounded-full inline-flex items-center justify-center font-bold" title="ลบขั้นตอน">✕</button>
                        </td>
                    `;
                    tableBody.appendChild(row);
                });
            }
        }

        // ==================== PAGE 1 HISTORY LOGIC ====================
        const TESTCASE_HISTORY_KEY = 'cat_testcase_history';

        window.saveTestCaseHistory = function () {
            if (testCases.length === 0) {
                alert('โปรดเพิ่ม Test Case ก่อนบันทึกนะแง้ว!');
                return;
            }
            const name = prompt('กรุณาตั้งชื่อชุด Test Case นี้:', testCases[0].name || 'ชุดทดสอบใหม่');
            if (name === null) return;

            const history = JSON.parse(localStorage.getItem(TESTCASE_HISTORY_KEY) || '[]');
            history.unshift({
                id: Date.now(),
                name: name || 'ไม่ได้ระบุชื่อ',
                date: new Date().toLocaleString('th-TH'),
                data: [...testCases]
            });
            localStorage.setItem(TESTCASE_HISTORY_KEY, JSON.stringify(history));
            renderTestCaseHistory();
            showToast('บันทึกประวัติสำเร็จ! 💾');
        };

        function renderTestCaseHistory() {
            const list = document.getElementById('testCaseHistoryList');
            const empty = document.getElementById('testCaseHistoryEmpty');
            const history = JSON.parse(localStorage.getItem(TESTCASE_HISTORY_KEY) || '[]');

            list.innerHTML = '';
            if (history.length === 0) {
                empty.classList.remove('hidden');
                return;
            }
            empty.classList.add('hidden');

            history.forEach((item, idx) => {
                const el = document.createElement('div');
                el.className = 'flex items-center justify-between bg-white border-2 border-rose-50 rounded-xl px-4 py-3 hover:border-rose-200 transition-all shadow-sm';
                el.innerHTML = `
                    <div class="flex items-center gap-3">
                        <span class="text-xl">📋</span>
                        <div>
                            <p class="font-bold text-gray-700 text-sm">${escapeHtml(item.name)}</p>
                            <p class="text-xs text-gray-400">${item.date} · ${item.data.length} ขั้นตอน</p>
                        </div>
                    </div>
                    <div class="flex gap-2">
                        <button onclick="loadTestCaseFromHistory(${idx})" class="bg-rose-50 text-rose-500 px-3 py-1 rounded-lg text-xs font-bold hover:bg-rose-100 transition-colors">
                            📂 ดึงข้อมูล
                        </button>
                        <button onclick="deleteTestCaseHistory(${idx})" class="text-gray-300 hover:text-red-400 p-1 transition-colors">
                            ✕
                        </button>
                    </div>
                `;
                list.appendChild(el);
            });
        }

        window.loadTestCaseFromHistory = function (idx) {
            const history = JSON.parse(localStorage.getItem(TESTCASE_HISTORY_KEY) || '[]');
            if (confirm('ต้องการดึงข้อมูลชุดนี้มาแทนที่ข้อมูลปัจจุบันใช่หรือไม่?')) {
                testCases = [...history[idx].data];
                renderTable();
                showToast('ดึงข้อมูลสำเร็จ! 🐾');
            }
        };

        window.deleteTestCaseHistory = function (idx) {
            if (!confirm('ยืนยันระบบลบบันทึกชุดนี้?')) return;
            const history = JSON.parse(localStorage.getItem(TESTCASE_HISTORY_KEY) || '[]');
            history.splice(idx, 1);
            localStorage.setItem(TESTCASE_HISTORY_KEY, JSON.stringify(history));
            renderTestCaseHistory();
        };

        renderTestCaseHistory();

        // Toast notification
        function showToast(message) {
            const toast = document.createElement('div');
            toast.className = 'fixed bottom-6 right-6 bg-gray-800 text-white px-6 py-3 rounded-xl shadow-2xl text-sm font-medium z-50 fade-in';
            toast.textContent = message;
            document.body.appendChild(toast);
            setTimeout(() => toast.remove(), 2500);
        }

        // ==================== PAGE 3: Test Coverage Matrix Logic ====================
        let matrixRequirements = [];
        let matrixTestCases = [];
        let matrixMapping = {}; // key: reqIdx (string), value: array of tcIdx (numbers)
        const MATRIX_HISTORY_KEY = 'cat_matrix_history';

        window.addRequirement = function () {
            const input = document.getElementById('reqInput');
            const val = input.value.trim();
            if (!val) return;
            matrixRequirements.push(val);
            input.value = '';
            renderMatrix();
            input.focus();
        };

        window.addMatrixTestCase = function () {
            const input = document.getElementById('matrixTcInput');
            const val = input.value.trim();
            if (!val) return;
            matrixTestCases.push(val);
            input.value = '';
            renderMatrix();
            input.focus();
        };

        window.pullTestCasesFromPage1 = function () {
            if (testCases.length === 0) {
                showToast('ไม่พบ Test Case ในหน้าแรกครับเมี้ยว 😿');
                return;
            }
            const tcNames = testCases.map(tc => tc.name).filter((val, idx, self) => val && self.indexOf(val) === idx);
            if (tcNames.length === 0) {
                showToast('ไม่พบชื่อ Test Case ที่สมบูรณ์ในหน้าแรก 😿');
                return;
            }
            if (matrixTestCases.length > 0 && !confirm('ต้องการดึงกรณีทดสอบมาเขียนทับรายการเดิมทั้งหมดใช่หรือไม่?\n(กดยกเลิกเพื่อเพิ่มต่อท้าย)')) {
                // Add unique only
                tcNames.forEach(name => {
                    if (!matrixTestCases.includes(name)) {
                        matrixTestCases.push(name);
                    }
                });
            } else {
                matrixTestCases = tcNames;
                matrixMapping = {};
            }
            renderMatrix();
            showToast('ดึงกรณีทดสอบจากหน้าแรกสำเร็จ! 🐾');
        };

        window.toggleMatrixMapping = function (reqIdx, tcIdx) {
            if (!matrixMapping[reqIdx]) {
                matrixMapping[reqIdx] = [];
            }
            const idx = matrixMapping[reqIdx].indexOf(tcIdx);
            if (idx > -1) {
                matrixMapping[reqIdx].splice(idx, 1);
            } else {
                matrixMapping[reqIdx].push(tcIdx);
            }
            renderMatrix();
        };

        window.updateRequirementText = function (reqIdx, text) {
            if (text.trim() === '') {
                deleteRequirement(reqIdx);
            } else {
                matrixRequirements[reqIdx] = text.trim();
                updateMatrixStats();
            }
        };

        window.updateMatrixTestCaseName = function (tcIdx, text) {
            if (text.trim() === '') {
                deleteMatrixTestCase(tcIdx);
            } else {
                matrixTestCases[tcIdx] = text.trim();
                updateMatrixStats();
            }
        };

        window.deleteRequirement = function (reqIdx) {
            matrixRequirements.splice(reqIdx, 1);
            
            // Adjust mapping keys
            const newMapping = {};
            for (const key in matrixMapping) {
                const kNum = parseInt(key);
                if (kNum === reqIdx) continue;
                if (kNum > reqIdx) {
                    newMapping[String(kNum - 1)] = matrixMapping[key];
                } else {
                    newMapping[key] = matrixMapping[key];
                }
            }
            matrixMapping = newMapping;
            renderMatrix();
        };

        window.deleteMatrixTestCase = function (tcIdx) {
            matrixTestCases.splice(tcIdx, 1);
            
            // Adjust mapping values
            for (const key in matrixMapping) {
                matrixMapping[key] = matrixMapping[key]
                    .filter(val => val !== tcIdx)
                    .map(val => val > tcIdx ? val - 1 : val);
            }
            renderMatrix();
        };

        window.updateMatrixStats = function () {
            const statTotalReqs = document.getElementById('statTotalReqs');
            const statTotalTcs = document.getElementById('statTotalTcs');
            const statCoveragePct = document.getElementById('statCoveragePct');
            
            if (!statTotalReqs || !statTotalTcs || !statCoveragePct) return;
            
            const totalReqs = matrixRequirements.length;
            const totalTcs = matrixTestCases.length;
            
            statTotalReqs.textContent = totalReqs;
            statTotalTcs.textContent = totalTcs;
            
            if (totalReqs === 0) {
                statCoveragePct.textContent = '0%';
                return;
            }
            
            let coveredReqsCount = 0;
            for (let i = 0; i < totalReqs; i++) {
                if (matrixMapping[i] && matrixMapping[i].length > 0) {
                    coveredReqsCount++;
                }
            }
            
            const pct = Math.round((coveredReqsCount / totalReqs) * 100);
            statCoveragePct.textContent = `${pct}%`;
        };

        window.resetMatrix = function (skipConfirm = false) {
            if (!skipConfirm && !confirm('คุณต้องการล้างข้อมูลความครอบคลุมทั้งหมดใช่หรือไม่?')) return;
            matrixRequirements = [];
            matrixTestCases = [];
            matrixMapping = {};
            renderMatrix();
            if (!skipConfirm) showToast('ล้างตารางเมทริกซ์สำเร็จ! 🧼');
        };

        window.requestAIAutoMap = function () {
            if (matrixRequirements.length === 0 || matrixTestCases.length === 0) {
                alert('โปรดเพิ่ม Test Scenario และเงื่อนไขก่อนขอให้คุมะวิเคราะห์จับคู่นะแง้ว!');
                return;
            }
            
            const messageText = `ช่วยวิเคราะห์เงื่อนไข (เงื่อนไข) และ Test Scenario ต่อไปนี้ แล้วส่ง JSON action "update_matrix" เพื่อจับคู่ความครอบคลุม (Test Coverage Mapping) ให้หน่อยครับเมี้ยว:
            
เงื่อนไข (Column):
${matrixRequirements.map((r, i) => `${i + 1}. ${r}`).join('\n')}

Test Scenario (Row):
${matrixTestCases.map((tc, i) => `${i + 1}. ${tc}`).join('\n')}`;

            if (!isAIOpen) {
                toggleAI();
            }
            
            const chatInput = document.getElementById('chat-input');
            chatInput.value = messageText;
            sendMessage();
        };

        window.copyForGoogleSheets = function () {
            if (matrixTestCases.length === 0) {
                alert('ยังไม่มีข้อมูล Test Scenario ในการคัดลอกครับ 😿');
                return;
            }
            
            let tsv = '';
            
            // Header Row
            const headers = ['ลำดับ', 'Test Scenario'];
            matrixRequirements.forEach(req => {
                headers.push(req);
            });
            tsv += headers.join('\t') + '\n';
            
            // Data Rows
            matrixTestCases.forEach((tc, tIdx) => {
                const row = [tIdx + 1, tc];
                matrixRequirements.forEach((req, rIdx) => {
                    const isMapped = matrixMapping[rIdx] && matrixMapping[rIdx].includes(tIdx);
                    row.push(isMapped ? '✔️' : '');
                });
                tsv += row.join('\t') + '\n';
            });
            
            // Add coverage footer row
            const footRow = ['ความครอบคลุม (เงื่อนไข)', ''];
            matrixRequirements.forEach((req, rIdx) => {
                const hasCover = matrixMapping[rIdx] && matrixMapping[rIdx].length > 0;
                footRow.push(hasCover ? 'ครอบคลุม' : 'ไม่ครอบคลุม');
            });
            tsv += footRow.join('\t') + '\n';
            
            navigator.clipboard.writeText(tsv).then(() => {
                showToast('คัดลอกเมทริกซ์ในรูปแบบ Google Sheets สำเร็จ! วาง (Ctrl+V) ได้เลยครับ 🟢🐾');
            }).catch(err => {
                console.error('TSV copy failed:', err);
                alert('ไม่สามารถคัดลอกได้: ' + err.message);
            });
        };

        async function exportMatrixExcel() {
            if (matrixTestCases.length === 0) {
                alert('ยังไม่มีข้อมูล Test Scenario ในการส่งออกครับ 😿');
                return;
            }
            try {
                const workbook = new ExcelJS.Workbook();
                const worksheet = workbook.addWorksheet('Test Coverage Matrix');
                
                const cols = [
                    { header: 'ลำดับ', key: 'no', width: 8 },
                    { header: 'Test Scenario', key: 'tc', width: 35 }
                ];
                matrixRequirements.forEach((req, idx) => {
                    cols.push({ header: req, key: `req_${idx}`, width: 15 });
                });
                worksheet.columns = cols;
                
                const headerRow = worksheet.getRow(1);
                headerRow.font = { bold: true, color: { argb: 'FFFFFFFF' } };
                headerRow.alignment = { vertical: 'middle', horizontal: 'center', wrapText: true };
                
                headerRow.eachCell((cell) => {
                    cell.fill = {
                        type: 'pattern',
                        pattern: 'solid',
                        fgColor: { argb: 'FFFDA4AF' }
                    };
                });
                
                matrixTestCases.forEach((tc, tIdx) => {
                    const rowData = { no: tIdx + 1, tc: tc };
                    matrixRequirements.forEach((req, rIdx) => {
                        const isMapped = matrixMapping[rIdx] && matrixMapping[rIdx].includes(tIdx);
                        rowData[`req_${rIdx}`] = isMapped ? '✔️' : '';
                    });
                    
                    const row = worksheet.addRow(rowData);
                    row.alignment = { vertical: 'middle' };
                    
                    row.getCell('no').alignment = { vertical: 'middle', horizontal: 'center' };
                    row.getCell('tc').alignment = { vertical: 'middle', horizontal: 'left', wrapText: true };
                    
                    matrixRequirements.forEach((req, rIdx) => {
                        const cell = row.getCell(`req_${rIdx}`);
                        cell.alignment = { vertical: 'middle', horizontal: 'center' };
                        if (cell.value === '✔️') {
                            cell.font = { bold: true, color: { argb: 'FF059669' } };
                            cell.fill = {
                                type: 'pattern',
                                pattern: 'solid',
                                fgColor: { argb: 'FFD1FAE5' }
                            };
                        }
                    });
                });
                
                // Add footer row in Excel
                const footRowData = { no: 'ความครอบคลุม (เงื่อนไข)', tc: '' };
                matrixRequirements.forEach((req, rIdx) => {
                    const hasCover = matrixMapping[rIdx] && matrixMapping[rIdx].length > 0;
                    footRowData[`req_${rIdx}`] = hasCover ? 'ครอบคลุม' : 'ไม่ครอบคลุม';
                });
                const footRow = worksheet.addRow(footRowData);
                footRow.alignment = { vertical: 'middle' };
                const tcCell = footRow.getCell('no');
                tcCell.font = { bold: true, color: { argb: 'FFBE185D' } }; // rose-700
                tcCell.fill = {
                    type: 'pattern',
                    pattern: 'solid',
                    fgColor: { argb: 'FFFDF2F8' } // rose-50
                };
                
                const tcDescCell = footRow.getCell('tc');
                tcDescCell.fill = {
                    type: 'pattern',
                    pattern: 'solid',
                    fgColor: { argb: 'FFFDF2F8' }
                };
                
                matrixRequirements.forEach((req, rIdx) => {
                    const cell = footRow.getCell(`req_${rIdx}`);
                    cell.alignment = { vertical: 'middle', horizontal: 'center' };
                    cell.font = { bold: true };
                    const hasCover = matrixMapping[rIdx] && matrixMapping[rIdx].length > 0;
                    if (hasCover) {
                        cell.font = { bold: true, color: { argb: 'FF059669' } };
                        cell.fill = {
                            type: 'pattern',
                            pattern: 'solid',
                            fgColor: { argb: 'FFD1FAE5' }
                        };
                    } else {
                        cell.font = { bold: true, color: { argb: 'FFDC2626' } };
                        cell.fill = {
                            type: 'pattern',
                            pattern: 'solid',
                            fgColor: { argb: 'FFFEE2E2' }
                        };
                    }
                });
                
                for (let r = 1; r <= worksheet.rowCount; r++) {
                    const row = worksheet.getRow(r);
                    for (let c = 1; c <= cols.length; c++) {
                        const cell = row.getCell(c);
                        cell.border = {
                            top: { style: 'thin', color: { argb: 'FFE2E8F0' } },
                            left: { style: 'thin', color: { argb: 'FFE2E8F0' } },
                            bottom: { style: 'thin', color: { argb: 'FFE2E8F0' } },
                            right: { style: 'thin', color: { argb: 'FFE2E8F0' } }
                        };
                    }
                }
                
                const buffer = await workbook.xlsx.writeBuffer();
                const blob = new Blob([buffer], { type: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet' });
                saveAs(blob, 'Cat_Test_Coverage_Matrix.xlsx');
                showToast('ส่งออก Excel Test Coverage Matrix สำเร็จ! 📊📥');
            } catch (error) {
                console.error('Matrix export failed:', error);
                alert('เกิดข้อผิดพลาดในการส่งออกไฟล์ Matrix: ' + error.message);
            }
        }

        window.renderMatrix = function () {
            const header = document.getElementById('matrixHeader');
            const body = document.getElementById('matrixBody');
            const emptyState = document.getElementById('matrixEmptyState');
            
            if (!header || !body || !emptyState) return;
            
            header.innerHTML = '';
            body.innerHTML = '';
            
            if (matrixRequirements.length === 0 && matrixTestCases.length === 0) {
                emptyState.classList.remove('hidden');
                updateMatrixStats();
                return;
            }
            emptyState.classList.add('hidden');
            
            const hr = document.createElement('tr');
            hr.className = 'border-b-2 border-rose-50 bg-rose-50/20';
            
            const thNo = document.createElement('th');
            thNo.className = 'py-3 px-4 text-rose-500 font-semibold text-sm w-16 text-center';
            thNo.textContent = 'ลำดับ';
            hr.appendChild(thNo);

            const thReq = document.createElement('th');
            thReq.className = 'py-3 px-4 text-rose-500 font-semibold text-sm w-1/3 border-l border-rose-50/50';
            thReq.textContent = 'Test Scenario';
            hr.appendChild(thReq);
            
            matrixRequirements.forEach((req, idx) => {
                const th = document.createElement('th');
                th.className = 'py-3 px-4 text-rose-400 font-semibold text-sm text-center border-l border-rose-50/50 min-w-[120px] relative group';
                
                const titleSpan = document.createElement('span');
                titleSpan.contentEditable = true;
                titleSpan.className = 'outline-none focus:bg-white focus:border focus:border-rose-300 px-1 rounded block';
                titleSpan.textContent = req;
                titleSpan.onblur = function() {
                    updateRequirementText(idx, this.innerText);
                };
                
                const delBtn = document.createElement('button');
                delBtn.className = 'absolute -top-1 -right-1 bg-red-100 text-red-500 rounded-full w-4 h-4 text-[9px] font-bold items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity';
                delBtn.textContent = '✕';
                delBtn.onclick = function(e) {
                    e.stopPropagation();
                    deleteRequirement(idx);
                };
                
                const wrapDiv = document.createElement('div');
                wrapDiv.className = 'relative inline-block pr-3 w-full';
                wrapDiv.appendChild(titleSpan);
                wrapDiv.appendChild(delBtn);
                
                th.appendChild(wrapDiv);
                hr.appendChild(th);
            });
            
            const thAct = document.createElement('th');
            thAct.className = 'py-3 px-4 text-rose-500 font-semibold text-sm text-center border-l border-rose-50/50 w-16';
            thAct.textContent = 'จัดการ';
            hr.appendChild(thAct);
            
            header.appendChild(hr);
            
            matrixTestCases.forEach((tc, tIdx) => {
                const tr = document.createElement('tr');
                tr.className = 'border-b border-rose-50 hover:bg-rose-50/30 transition-colors';
                
                const tdNo = document.createElement('td');
                tdNo.className = 'py-3 px-4 font-medium text-gray-400 text-sm text-center w-16';
                tdNo.textContent = tIdx + 1;
                tr.appendChild(tdNo);

                const tdReq = document.createElement('td');
                tdReq.className = 'py-3 px-4 font-medium text-gray-700 text-sm border-l border-rose-50/50';
                tdReq.contentEditable = true;
                tdReq.textContent = tc;
                tdReq.onblur = function() {
                    updateMatrixTestCaseName(tIdx, this.innerText);
                };
                tr.appendChild(tdReq);
                
                matrixRequirements.forEach((req, rIdx) => {
                    const td = document.createElement('td');
                    td.className = 'py-3 px-4 text-center border-l border-rose-50/50 cursor-pointer select-none text-xl hover:bg-rose-100/30 transition-colors';
                    
                    const isMapped = matrixMapping[rIdx] && matrixMapping[rIdx].includes(tIdx);
                    if (isMapped) {
                        td.textContent = '🐾';
                        td.className += ' text-rose-400 bg-rose-50/20';
                    }
                    
                    td.onclick = function() {
                        toggleMatrixMapping(rIdx, tIdx);
                    };
                    tr.appendChild(td);
                });
                
                const tdAct = document.createElement('td');
                tdAct.className = 'py-3 px-4 text-center border-l border-rose-50/50';
                tdAct.innerHTML = `
                    <button onclick="deleteMatrixTestCase(${tIdx})" class="btn-danger w-6 h-6 rounded-full inline-flex items-center justify-center font-bold text-xs" title="ลบ Test Scenario">✕</button>
                `;
                tr.appendChild(tdAct);
                
                body.appendChild(tr);
            });
            
            if (matrixTestCases.length > 0 && matrixRequirements.length > 0) {
                const trFoot = document.createElement('tr');
                trFoot.className = 'bg-rose-50/10 font-bold border-t-2 border-rose-50';
                
                const tdLabel = document.createElement('td');
                tdLabel.className = 'py-3 px-4 text-rose-500 text-sm';
                tdLabel.colSpan = 2;
                tdLabel.textContent = 'ความครอบคลุม (เงื่อนไข)';
                trFoot.appendChild(tdLabel);
                
                matrixRequirements.forEach((req, rIdx) => {
                    const td = document.createElement('td');
                    td.className = 'py-3 px-4 text-center border-l border-rose-50/50 text-xs';
                    const hasCover = matrixMapping[rIdx] && matrixMapping[rIdx].length > 0;
                    if (hasCover) {
                        td.innerHTML = '<span class="text-green-500 bg-green-50 px-2 py-1 rounded-full">ครอบคลุม ✔️</span>';
                    } else {
                        td.innerHTML = '<span class="text-rose-400 bg-rose-50 px-2 py-1 rounded-full">ไม่ครอบคลุม ✕</span>';
                    }
                    trFoot.appendChild(td);
                });
                
                const tdActEmpty = document.createElement('td');
                tdActEmpty.className = 'py-3 px-4 border-l border-rose-50/50';
                trFoot.appendChild(tdActEmpty);
                
                body.appendChild(trFoot);
            }
            
            updateMatrixStats();
        };

        window.saveMatrixHistory = function () {
            if (matrixRequirements.length === 0 && matrixTestCases.length === 0) {
                alert('โปรดระบุข้อมูล Matrix ก่อนบันทึกนะแง้ว!');
                return;
            }
            const name = prompt('กรุณาตั้งชื่อชุด Test Coverage Matrix นี้:', matrixRequirements[0] ? `เมทริกซ์สำหรับ ${matrixRequirements[0]}` : 'เมทริกซ์ใหม่');
            if (name === null) return;
            
            const history = JSON.parse(localStorage.getItem(MATRIX_HISTORY_KEY) || '[]');
            history.unshift({
                id: Date.now(),
                name: name || 'ไม่ได้ระบุชื่อ',
                date: new Date().toLocaleString('th-TH'),
                requirements: [...matrixRequirements],
                testcases: [...matrixTestCases],
                mapping: JSON.parse(JSON.stringify(matrixMapping))
            });
            localStorage.setItem(MATRIX_HISTORY_KEY, JSON.stringify(history));
            renderMatrixHistory();
            showToast('บันทึกประวัติเมทริกซ์สำเร็จ! 💾');
        };

        function renderMatrixHistory() {
            const list = document.getElementById('matrixHistoryList');
            const empty = document.getElementById('matrixHistoryEmpty');
            if (!list || !empty) return;
            const history = JSON.parse(localStorage.getItem(MATRIX_HISTORY_KEY) || '[]');
            
            list.innerHTML = '';
            if (history.length === 0) {
                empty.classList.remove('hidden');
                return;
            }
            empty.classList.add('hidden');
            
            history.forEach((item, idx) => {
                const el = document.createElement('div');
                el.className = 'flex items-center justify-between bg-white border-2 border-rose-50 rounded-xl px-4 py-3 hover:border-rose-200 transition-all shadow-sm';
                el.innerHTML = `
                    <div class="flex items-center gap-3">
                        <span class="text-xl">📊</span>
                        <div>
                            <p class="font-bold text-gray-700 text-sm">${escapeHtml(item.name)}</p>
                            <p class="text-xs text-gray-400">${item.date} · ${item.requirements.length} เงื่อนไข · ${item.testcases.length} Scenario</p>
                        </div>
                    </div>
                    <div class="flex gap-2">
                        <button onclick="loadMatrixFromHistory(${idx})" class="bg-rose-50 text-rose-500 px-3 py-1 rounded-lg text-xs font-bold hover:bg-rose-100 transition-colors">
                            📂 ดึงข้อมูล
                        </button>
                        <button onclick="deleteMatrixHistory(${idx})" class="text-gray-300 hover:text-red-400 p-1 transition-colors">
                            ✕
                        </button>
                    </div>
                `;
                list.appendChild(el);
            });
        }

        window.loadMatrixFromHistory = function (idx) {
            const history = JSON.parse(localStorage.getItem(MATRIX_HISTORY_KEY) || '[]');
            if (confirm('ต้องการดึงข้อมูลชุดนี้มาแทนที่ข้อมูลปัจจุบันใช่หรือไม่?')) {
                matrixRequirements = [...history[idx].requirements];
                matrixTestCases = [...history[idx].testcases];
                matrixMapping = JSON.parse(JSON.stringify(history[idx].mapping));
                renderMatrix();
                showToast('ดึงข้อมูลเมทริกซ์สำเร็จ! 🐾');
            }
        };

        window.deleteMatrixHistory = function (idx) {
            if (!confirm('ยืนยันระบบลบบันทึกเมทริกซ์ชุดนี้?')) return;
            const history = JSON.parse(localStorage.getItem(MATRIX_HISTORY_KEY) || '[]');
            history.splice(idx, 1);
            localStorage.setItem(MATRIX_HISTORY_KEY, JSON.stringify(history));
            renderMatrixHistory();
            showToast('ลบประวัติแล้ว');
        };
        
        setTimeout(() => {
            const reqInput = document.getElementById('reqInput');
            const matrixTcInput = document.getElementById('matrixTcInput');
            if (reqInput) {
                reqInput.addEventListener('keydown', (e) => {
                    if (e.key === 'Enter') addRequirement();
                });
            }
            if (matrixTcInput) {
                matrixTcInput.addEventListener('keydown', (e) => {
                    if (e.key === 'Enter') addMatrixTestCase();
                });
            }
            renderMatrixHistory();
            renderMatrix();
        }, 500);
    </script>

    <!-- KUMA Desktop Pet -->
    <div id="kuma-pet" class="hidden-cat">
        <div class="kuma-bubble">เหมียว~ 🐾</div>
        <div class="kuma-wrapper" style="width: 70px; height: 55px; position: relative; transition: transform 0.2s ease;">
            <div class="kuma-body">
                <div class="head">
                    <div class="ear left"></div>
                    <div class="ear right"></div>
                    <div class="eye left"></div>
                    <div class="eye right"></div>
                    <div class="blush left"></div>
                    <div class="blush right"></div>
                    <div class="nose"></div>
                    <div class="mouth"></div>
                </div>
                <div class="body"></div>
                <div class="tail"></div>
                <div class="leg fl"></div>
                <div class="leg fr"></div>
                <div class="leg bl"></div>
                <div class="leg br"></div>
                <span class="zzz">💤</span>
            </div>
        </div>
    </div>

    <a href="/members" id="cat-friends-btn"
        class="fixed top-[15px] right-[15px] z-[1000] px-4 py-2 rounded-full font-bold text-xs border-2 border-amber-300 text-amber-600 hover:bg-amber-50 hover:shadow-lg hover:-translate-y-0.5 transition-all flex items-center gap-1 shadow-md bg-white/95 backdrop-blur"
        title="สำหรับเพื่อนสนิทน้องคุมะ">
        🔐 สำหรับเพื่อนสนิทน้องคุมะ
    </a>

    <div id="cat-house-btn" onclick="toggleCatHouse()">
        🏠
        <span class="tooltip">เรียกคุมะออกมาเล่น!</span>
    </div>

    <script>
        (function () {
            const pet = document.getElementById('kuma-pet');
            const houseBtn = document.getElementById('cat-house-btn');
            const bubble = pet.querySelector('.kuma-bubble');
            let catOut = false;
            let isDragging = false;
            let walkInterval = null;
            let idleTimeout = null;
            let sleepTimeout = null;
            let bubbleTimeout = null;
            let happyTimeout = null;
            let dragMoved = false;
            let posX = 100, posY = 60;
            let dirX = 1; // 1 = right, -1 = left
            let speed = 1.5;
            let dragOffsetX = 0, dragOffsetY = 0;

            function sayBubble(text, duration = 3000) {
                if (bubbleTimeout) clearTimeout(bubbleTimeout);
                bubble.textContent = text;
                pet.classList.add('saying');
                
                bubbleTimeout = setTimeout(() => {
                    pet.classList.remove('saying');
                }, duration);
            }

            function createFloatingEmoji(emoji) {
                const el = document.createElement('span');
                el.textContent = emoji;
                el.style.position = 'absolute';
                el.style.top = '-10px';
                el.style.left = '50%';
                el.style.transform = 'translateX(-50%)';
                el.style.fontSize = '18px';
                el.style.pointerEvents = 'none';
                el.style.zIndex = '9999';
                el.style.transition = 'all 1s ease-out';
                pet.appendChild(el);
                
                setTimeout(() => {
                    el.style.transform = 'translate(-50%, -45px) scale(1.4)';
                    el.style.opacity = '0';
                }, 50);
                
                setTimeout(() => {
                    el.remove();
                }, 1000);
            }

            window.petCat = function () {
                if (isDragging) return;
                
                // Wake up if sleeping
                if (pet.classList.contains('sleeping')) {
                    pet.classList.remove('sleeping');
                    startWalking();
                    sayBubble('ฮ้าววว~ ตื่นแล้วครับคุณแม่มนุษย์! 🥱🐾');
                    return;
                }
                
                // Stop walking briefly
                stopWalking();
                pet.classList.remove('walking');
                pet.classList.add('happy');
                
                const quotes = [
                    'เหมียว~ 🐾',
                    'คุมะรักคุณแม่มนุษย์นะเหมียว~ ❤️',
                    'วันนี้ทำเก่งมากเลยครับ! 💻',
                    'คุมะสแตนด์บายตรวจเทสอยู่ครับ! 🔎🐾',
                    'เกาพุงคุมะอีกๆ เมี้ยว~ 🥰',
                    'มีอะไรให้คุมะช่วยเขียนเทสบอกได้เลยครับแง้ว! 🦊'
                ];
                const randQuote = quotes[Math.floor(Math.random() * quotes.length)];
                sayBubble(randQuote);
                
                if (happyTimeout) clearTimeout(happyTimeout);
                happyTimeout = setTimeout(() => {
                    pet.classList.remove('happy');
                    if (catOut && !isDragging) startWalking();
                }, 1500);
            };

            // Toggle cat house
            window.toggleCatHouse = function () {
                if (catOut) {
                    // Send cat home
                    catOut = false;
                    stopWalking();
                    pet.classList.remove('walking', 'sleeping', 'entering');
                    pet.classList.add('hidden-cat');
                    houseBtn.innerHTML = '🏠<span class="tooltip">เรียกคุมะออกมาเล่น!</span>';
                } else {
                    // Let cat out
                    catOut = true;
                    pet.classList.remove('hidden-cat');
                    pet.classList.add('entering');
                    pet.style.left = posX + 'px';
                    pet.style.bottom = posY + 'px';
                    houseBtn.innerHTML = '🏠<span class="tooltip">ให้คุมะเข้าบ้าน</span>';
                    setTimeout(() => {
                        pet.classList.remove('entering');
                        startWalking();
                    }, 700);
                }
            };

            function startWalking() {
                if (walkInterval) clearInterval(walkInterval);
                pet.classList.remove('sleeping');
                pet.classList.add('walking');
                resetIdleTimer();

                walkInterval = setInterval(() => {
                    if (isDragging || !catOut) return;

                    posX += dirX * speed;

                    // Bounce off viewport edges
                    const maxX = window.innerWidth - 80;
                    if (posX <= 10) { posX = 10; dirX = 1; }
                    if (posX >= maxX) { posX = maxX; dirX = -1; }

                    pet.style.left = posX + 'px';
                    const wrapper = pet.querySelector('.kuma-wrapper');
                    if (wrapper) wrapper.style.transform = dirX < 0 ? 'scaleX(1)' : 'scaleX(-1)';
                }, 30);

                // Randomly change direction
                scheduleDirectionChange();
            }

            function scheduleDirectionChange() {
                const next = 2000 + Math.random() * 5000;
                setTimeout(() => {
                    if (!catOut || isDragging) return;
                    // Randomly: change direction, pause, or keep going
                    const action = Math.random();
                    if (action < 0.3) {
                        dirX *= -1; // flip
                    } else if (action < 0.5) {
                        // Pause briefly
                        stopWalking();
                        pet.classList.remove('walking');
                        setTimeout(() => {
                            if (catOut && !isDragging) startWalking();
                        }, 1500 + Math.random() * 2000);
                        return;
                    }
                    if (catOut) scheduleDirectionChange();
                }, next);
            }

            function stopWalking() {
                if (walkInterval) { clearInterval(walkInterval); walkInterval = null; }
            }

            function showRandomMascotState() {
                if (!catOut || isDragging || pet.classList.contains('sleeping') || pet.classList.contains('happy')) return;
                const idleQuotes = [
                    'ฮึบๆ เดินออกกำลังกายซะหน่อย 🐾',
                    'คุมะพร้อมตรวจงานแล้วครับ! 🔎',
                    'แอบมองคุณแม่มนุษย์อยู่นะแง้ว~ 👀',
                    'สู้ๆ นะครับ! คุมะเป็นกำลังใจให้ 💖',
                    'คัดลอกตารางไปวางใน Google Sheets ได้เลยนะเหมียว! 🟢'
                ];
                const randQuote = idleQuotes[Math.floor(Math.random() * idleQuotes.length)];
                sayBubble(randQuote, 2500);
            }

            function resetIdleTimer() {
                if (idleTimeout) clearTimeout(idleTimeout);
                if (sleepTimeout) clearTimeout(sleepTimeout);
                pet.classList.remove('sleeping');

                // Periodically show thoughts when walking
                idleTimeout = setInterval(() => {
                    if (Math.random() < 0.4) {
                        showRandomMascotState();
                    }
                }, 12000);

                sleepTimeout = setTimeout(() => {
                    if (!isDragging && catOut) {
                        stopWalking();
                        if (idleTimeout) clearInterval(idleTimeout);
                        pet.classList.remove('walking');
                        pet.classList.add('sleeping');
                        sayBubble('ฟี้... ครอก... 😴', 4000);
                    }
                }, 20000 + Math.random() * 10000);
            }

            // ---- Drag & Drop ----
            function startDrag(e) {
                if (!catOut) return;
                isDragging = true;
                dragMoved = false;
                pet.classList.add('dragging');
                pet.classList.remove('walking', 'sleeping');
                stopWalking();

                const clientX = e.touches ? e.touches[0].clientX : e.clientX;
                const clientY = e.touches ? e.touches[0].clientY : e.clientY;
                const rect = pet.getBoundingClientRect();
                dragOffsetX = clientX - rect.left;
                dragOffsetY = clientY - rect.top;

                e.preventDefault();
            }

            function moveDrag(e) {
                if (!isDragging) return;
                dragMoved = true;
                const clientX = e.touches ? e.touches[0].clientX : e.clientX;
                const clientY = e.touches ? e.touches[0].clientY : e.clientY;

                posX = clientX - dragOffsetX;
                posY = window.innerHeight - clientY - (55 - dragOffsetY);

                // Clamp
                posX = Math.max(0, Math.min(window.innerWidth - 80, posX));
                posY = Math.max(0, Math.min(window.innerHeight - 80, posY));

                pet.style.left = posX + 'px';
                pet.style.bottom = posY + 'px';
                e.preventDefault();
            }

            function endDrag() {
                if (!isDragging) return;
                isDragging = false;
                pet.classList.remove('dragging');
                setTimeout(() => {
                    if (catOut) startWalking();
                }, 500);
            }

            pet.addEventListener('mousedown', startDrag);
            document.addEventListener('mousemove', moveDrag);
            document.addEventListener('mouseup', endDrag);
            pet.addEventListener('touchstart', startDrag, { passive: false });
            document.addEventListener('touchmove', moveDrag, { passive: false });
            document.addEventListener('touchend', endDrag);

            pet.addEventListener('click', () => {
                if (dragMoved) return;
                petCat();
            });

            pet.addEventListener('dblclick', () => {
                if (isDragging) return;
                
                stopWalking();
                pet.classList.remove('walking', 'sleeping');
                pet.classList.add('happy');
                
                const fishQuotes = [
                    'ง่ำๆๆๆ! อร่อยสุดยอดเลยเหมียว! 🐟❤️',
                    'ปลาทูตัวนี้หวานเจี๊ยบ! ขอบคุณนะเหมี้ยว~ 🥰',
                    'งับ! คุมะอิ่มแปร้ พร้อมรันเทสแล้วครับ! ⚡',
                    'แง๊ววว~ ของโปรดคุมะเลย! 😻🐾'
                ];
                const randQuote = fishQuotes[Math.floor(Math.random() * fishQuotes.length)];
                sayBubble(randQuote);
                
                createFloatingEmoji('🐟');
                setTimeout(() => createFloatingEmoji('❤️'), 300);
                
                if (happyTimeout) clearTimeout(happyTimeout);
                happyTimeout = setTimeout(() => {
                    pet.classList.remove('happy');
                    if (catOut && !isDragging) startWalking();
                }, 2000);
            });
        })();
    </script>

    <!-- Visitor Counter Widget -->
    <div class="absolute bottom-4 right-4 bg-white/80 backdrop-blur-md border border-rose-100 rounded-2xl px-4 py-3 shadow-lg z-50 text-xs font-semibold text-gray-600 flex items-center gap-3 transition-transform hover:scale-105 duration-200">
        <div class="flex items-center justify-center w-8 h-8 rounded-full bg-rose-50 text-rose-500 text-lg">
            🐾
        </div>
        <div class="grid grid-cols-3 gap-x-4 divide-x divide-rose-100/50">
            <div class="pl-0">
                <span class="block text-[10px] text-gray-400 uppercase tracking-wider">ทั้งหมด</span>
                <span id="visitor-total" class="text-rose-500 font-bold text-sm">0</span>
            </div>
            <div class="pl-4">
                <span class="block text-[10px] text-gray-400 uppercase tracking-wider">วันนี้</span>
                <span id="visitor-today" class="text-rose-600 font-bold text-sm">0</span>
            </div>
            <div class="pl-4">
                <span class="block text-[10px] text-gray-400 uppercase tracking-wider">ขณะนี้</span>
                <span id="visitor-active" class="text-green-500 font-bold text-sm">0</span>
            </div>
        </div>
    </div>

    <script>
        // ==================== Visitor Statistics Heartbeat ====================
        (function() {
            // Use localStorage so the same browser/device shares the same ID across tabs
            let visitorId = localStorage.getItem('kuma_visitor_id');
            if (!visitorId) {
                visitorId = 'visitor_' + Date.now() + '_' + Math.random().toString(36).substr(2, 9);
                localStorage.setItem('kuma_visitor_id', visitorId);
            }
            
            // Use sessionStorage to detect a new browser session/tab launch for counting
            let isNewSession = false;
            if (!sessionStorage.getItem('kuma_session_active')) {
                sessionStorage.setItem('kuma_session_active', 'true');
                isNewSession = true;
            }
            
            async function sendHeartbeat() {
                try {
                    const res = await fetch('/api/visitor-heartbeat', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                        body: JSON.stringify({ visitorId, isNewSession })
                    });
                    
                    isNewSession = false;
                    
                    const data = await res.json();
                    if (data.total !== undefined) {
                        document.getElementById('visitor-total').textContent = data.total;
                        document.getElementById('visitor-today').textContent = data.today;
                        document.getElementById('visitor-active').textContent = data.active;
                    }
                } catch (err) {
                    console.error('Visitor heartbeat failed:', err);
                }
            }
            
            sendHeartbeat();
            setInterval(sendHeartbeat, 20000);
        })();
    </script>

    <!-- Home Interactive Sections (Games & Fortune Telling) -->
    <script>
        // ==================== Home Interactive Sections ====================
        window.toggleHomeSection = function(section) {
            const container = document.getElementById('homeSectionsContainer');
            const fortune = document.getElementById('sectionFortune');
            const offline = document.getElementById('sectionOfflineGame');
            const online = document.getElementById('sectionOnlineGame');
            
            container.classList.remove('hidden');
            fortune.classList.add('hidden');
            offline.classList.add('hidden');
            online.classList.add('hidden');
            
            // Stop offline game loop if active
            if (window.stopOfflineGame) window.stopOfflineGame();
            
            if (section === 'fortune') {
                fortune.classList.remove('hidden');
            } else if (section === 'offlineGame') {
                offline.classList.remove('hidden');
            } else if (section === 'onlineGame') {
                online.classList.remove('hidden');
            }
            
            // Scroll smoothly to section
            setTimeout(() => {
                container.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
            }, 100);
        };
        
        // --- Fortune Telling Code ---
        const fortunes = {
            love: [
                "ความรักช่วงนี้เหมือนขนมแมวเลีย! หอมหวาน ยั่วยวนใจ มีแต่คนอยากวิ่งเข้าหา ใครโสดอยู่จะมีคนมาอ้อนดั่งลูกแมวตัวน้อย 🥰",
                "วันนี้ถ้าคุยกับแฟน ให้พูดลงท้ายด้วยคำว่า 'เมี้ยว~' หรือ 'แง้ว~' แล้วแฟนจะรักแฟนจะหลงตามใจทุกอย่างเลยเหมียว ❤️",
                "ดวงความรักสัปดาห์นี้ให้ระวังอาการขี้อ้อนเกินเหตุจนแฟนหมั่นเขี้ยว แฟนอาจจะแกล้งหยิกแก้มหรือกอดแน่นๆ จนหายใจไม่ออกนะแง้ว!",
                "คนโสดมีเกณฑ์เจอเนื้อคู่ในที่ทำงานหรือร้านกาแฟ ลองสบตาเขาสัก 3 วินาทีแล้วกระพริบตาช้าๆ แบบแมวส่งรักดูนะเหมียว! 😉"
            ],
            work: [
                "การเรียนการงานวันนี้พุ่งกระฉูดเหมือนแมวตอนตีสอง! สมองแล่นปรี๊ด เขียนโค้ดไม่มีติดบั๊ก ส่งงานผ่านฉลุยดั่งสายฟ้าฟาด ⚡💻",
                "งานที่ยากจะสำเร็จได้ง่ายๆ วันนี้คุมะแผ่พลังงานความโชคดีให้แล้ว ลุยเลยเหมียว! หัวหน้าจะเอ็นดูเหมือนทาสแมวเอ็นดูเจ้านาย",
                "ช่วงนี้อาจจะรู้สึกเบื่อและอยากนอนเฉยๆ ตลอดวัน (ดั่งวิถีแมวส้ม) แนะนำให้พักสักนิดแล้วหาขนมอร่อยๆ ทาน สมองจะแล่นขึ้นนะแง้ว!",
                "มีเกณฑ์ได้รับมอบหมายงานใหม่ที่ท้าทายความสามารถ แต่อย่ากลัวไปเลยครับ! คุมะการันตีว่าคุณแม่มนุษย์ทำได้ยอดเยี่ยมแน่นอน ✨"
            ],
            money: [
                "ดวงการเงินโดดเด่นมากกก! มีโชคลาภลอยแบบไม่คาดฝัน เลขนำโชคของคุณวันนี้คือเลขตีนแมว! ลองเสี่ยงโชคดูนะเหมียว 💵🎰",
                "มีเกณฑ์เสียทรัพย์ให้กับของน่ารักๆ หรืออาหารแมวอร่อยๆ แต่ไม่ต้องห่วง! กระเป๋าเงินคุณจะได้รับการเติมเต็มในเร็ววัน เมี้ยว!",
                "วันนี้กระเป๋าเงินตุง! เงินไหลเข้าดั่งกระแสน้ำวน แต่อย่าเพิ่งช็อปปิ้งออนไลน์ตอนดึกๆ นะแง้ว ไม่งั้นเงินจะปลิวไปดั่งใบไม้ร่วง 🍂",
                "ลาภลอยวันนี้ลองขอพรจากรูปปั้นแมวกวักหรือเกาพุงคุมะ 3 ที ท่องนะโม 3 จบดวงจะเฮงคูณสองเลยเหมียว! 🔮"
            ],
            health: [
                "สุขภาพแข็งแรงกระปรี้กระเปร่าดั่งแมวหนุ่ม! แต่ให้ระวังอาการปวดหลัง/ออฟฟิศซินโดรมจากการนั่งหลังขดหลังแข็งเขียนโค้ดนานๆ 🍀",
                "วันนี้ดวงสุขภาพดีมาก แต่ต้องดื่มน้ำเยอะๆ และอย่าลืมยืดเส้นยืดสาย บิดขี้เกียจเหมือนท่าแมวโยคะสัก 2 นาทีนะเหมียว 🧘‍♀️",
                "ระวังการนอนดึกจนตาเป็นแพนด้า! พักผ่อนให้เพียงพอเหมือนน้องคุมะที่หลับวันละ 16 ชั่วโมง แล้วผิวพรรณจะเปล่งปลั่งมีออร่าแง้ว 💤",
                "ระบบย่อยอาหารดีเลิศ แต่ระวังอาการกินจุเพราะความเครียด หากหิวให้เคี้ยวช้าๆ และหลีกเลี่ยงของมันของทอดนะเมี้ยว 🐟"
            ]
        };
        
        window.getFortune = function() {
            const cat = document.getElementById('fortuneCategory').value;
            const display = document.getElementById('fortuneResult');
            if (!display) return;
            
            display.classList.remove('hidden');
            display.innerHTML = `
                <div class="text-center py-4 text-purple-400">
                    <span class="inline-block animate-bounce text-2xl">🔮</span> คุมะกำลังเพ่งกระแสจิตเสี่ยงทายดวงชะตาให้...
                </div>
            `;
            
            // Play happy animation on Kuma mascot temporarily if available
            const pet = document.getElementById('kuma-pet');
            if (pet) {
                pet.classList.remove('walking', 'sleeping');
                pet.classList.add('happy');
                setTimeout(() => pet.classList.remove('happy'), 1500);
            }
            
            setTimeout(() => {
                const list = fortunes[cat];
                const randIdx = Math.floor(Math.random() * list.length);
                const prediction = list[randIdx];
                const luckyNumber = Math.floor(Math.random() * 9) + 1;
                const colors = ["ชมพูพาสเทล 🌸", "ส้มคุมะ 🍊", "ฟ้าสดใส 🐬", "ทองร่ำรวย 💰", "แดงโชคดี 🎈", "เขียวเหนี่ยวทรัพย์ 🍀"];
                const luckyColor = colors[Math.floor(Math.random() * colors.length)];
                
                display.innerHTML = `
                    <div class="flex items-start gap-3">
                        <div class="text-3xl">🔮</div>
                        <div>
                            <p class="font-semibold text-purple-700 text-sm leading-relaxed">${prediction}</p>
                            <div class="grid grid-cols-2 gap-2 mt-3 pt-3 border-t border-purple-100/50 text-xs text-gray-500 font-bold text-left">
                                <div>🍀 เลขนำโชค: <span class="text-purple-600 text-sm">${luckyNumber}</span></div>
                                <div>🎨 สีนำโชค: <span class="text-purple-600">${luckyColor}</span></div>
                            </div>
                        </div>
                    </div>
                `;
            }, 1200);
        };
        
        // --- Offline Game: Catch the Fish ---
        let gameCanvas, ctx;
        let gameActive = false;
        let gameLoopId = null;
        let basketX = 175;
        const basketWidth = 60;
        const basketHeight = 40;
        let score = 0;
        let lives = 3;
        let gameItems = [];
        let spawnTimer = 0;
        
        function drawKumaBasket(x, y) {
            ctx.fillStyle = '#f59e0b';
            ctx.beginPath();
            ctx.ellipse(x + basketWidth/2, y + basketHeight/2, basketWidth/2, basketHeight/2, 0, 0, 2 * Math.PI);
            ctx.fill();
            
            ctx.fillStyle = '#f59e0b';
            ctx.beginPath();
            ctx.arc(x + 10, y + 5, 8, 0, 2 * Math.PI);
            ctx.arc(x + basketWidth - 10, y + 5, 8, 0, 2 * Math.PI);
            ctx.fill();
            
            ctx.fillStyle = '#fee2e2';
            ctx.beginPath();
            ctx.arc(x + 10, y + 5, 4, 0, 2 * Math.PI);
            ctx.arc(x + basketWidth - 10, y + 5, 4, 0, 2 * Math.PI);
            ctx.fill();
            
            ctx.fillStyle = '#1f2937';
            ctx.beginPath();
            ctx.arc(x + 20, y + 18, 3, 0, 2 * Math.PI);
            ctx.arc(x + basketWidth - 20, y + 18, 3, 0, 2 * Math.PI);
            ctx.fill();
            
            ctx.fillStyle = '#1f2937';
            ctx.beginPath();
            ctx.arc(x + basketWidth/2, y + 23, 2, 0, 2 * Math.PI);
            ctx.fill();
        }
        
        window.initOfflineGame = function() {
            gameCanvas = document.getElementById('offlineGameCanvas');
            if (!gameCanvas) return;
            ctx = gameCanvas.getContext('2d');
            
            gameCanvas.addEventListener('mousemove', (e) => {
                const rect = gameCanvas.getBoundingClientRect();
                const mouseX = e.clientX - rect.left;
                basketX = mouseX - basketWidth / 2;
                if (basketX < 0) basketX = 0;
                if (basketX > gameCanvas.width - basketWidth) basketX = gameCanvas.width - basketWidth;
            });
            
            gameCanvas.addEventListener('touchmove', (e) => {
                const rect = gameCanvas.getBoundingClientRect();
                const touchX = e.touches[0].clientX - rect.left;
                basketX = touchX - basketWidth / 2;
                if (basketX < 0) basketX = 0;
                if (basketX > gameCanvas.width - basketWidth) basketX = gameCanvas.width - basketWidth;
                e.preventDefault();
            });

            score = 0;
            lives = 3;
            gameItems = [];
            gameActive = true;
            document.getElementById('gameScore').textContent = score;
            document.getElementById('gameLives').textContent = '❤️❤️❤️';
            document.getElementById('startGameBtn').textContent = 'เริ่มใหม่ 🎮';
            
            window.stopOfflineGame();
            gameLoop();
        };
        
        window.stopOfflineGame = function() {
            gameActive = false;
            if (gameLoopId) {
                cancelAnimationFrame(gameLoopId);
                gameLoopId = null;
            }
        };
        
        function spawnItem() {
            if (!gameCanvas) return;
            const types = ['fish', 'fish', 'fish', 'bomb', 'crab'];
            const randType = types[Math.floor(Math.random() * types.length)];
            gameItems.push({
                x: Math.random() * (gameCanvas.width - 30) + 15,
                y: -20,
                speed: 2 + Math.random() * 3 + (score / 150),
                type: randType,
                size: 20
            });
        }
        
        function gameLoop() {
            if (!gameActive) return;
            
            ctx.clearRect(0, 0, gameCanvas.width, gameCanvas.height);
            
            spawnTimer++;
            if (spawnTimer > 35) {
                spawnItem();
                spawnTimer = 0;
            }
            
            drawKumaBasket(basketX, gameCanvas.height - basketHeight - 10);
            
            for (let i = gameItems.length - 1; i >= 0; i--) {
                const item = gameItems[i];
                item.y += item.speed;
                
                ctx.beginPath();
                if (item.type === 'fish') {
                    ctx.fillStyle = '#60a5fa';
                    ctx.ellipse(item.x, item.y, 12, 6, 0, 0, 2 * Math.PI);
                    ctx.fill();
                    ctx.beginPath();
                    ctx.moveTo(item.x - 12, item.y);
                    ctx.lineTo(item.x - 18, item.y - 6);
                    ctx.lineTo(item.x - 18, item.y + 6);
                    ctx.fill();
                } else if (item.type === 'bomb') {
                    ctx.fillStyle = '#374151';
                    ctx.arc(item.x, item.y, 10, 0, 2 * Math.PI);
                    ctx.fill();
                    ctx.strokeStyle = '#ef4444';
                    ctx.lineWidth = 2;
                    ctx.beginPath();
                    ctx.moveTo(item.x, item.y - 10);
                    ctx.quadraticCurveTo(item.x + 5, item.y - 15, item.x + 3, item.y - 18);
                    ctx.stroke();
                } else if (item.type === 'crab') {
                    ctx.fillStyle = '#f87171';
                    ctx.arc(item.x, item.y, 10, 0, Math.PI, true);
                    ctx.fill();
                    ctx.lineWidth = 2;
                    ctx.strokeStyle = '#f87171';
                    ctx.beginPath();
                    ctx.moveTo(item.x - 8, item.y); ctx.lineTo(item.x - 14, item.y + 6);
                    ctx.moveTo(item.x + 8, item.y); ctx.lineTo(item.x + 14, item.y + 6);
                    ctx.stroke();
                }
                
                const basketY = gameCanvas.height - basketHeight - 10;
                if (item.y + 10 >= basketY && item.y - 10 <= basketY + basketHeight &&
                    item.x + 10 >= basketX && item.x - 10 <= basketX + basketWidth) {
                    
                    if (item.type === 'fish') {
                        score += 10;
                        document.getElementById('gameScore').textContent = score;
                    } else {
                        lives--;
                        let hearts = '';
                        for (let j = 0; j < lives; j++) hearts += '❤️';
                        if (hearts === '') hearts = 'ไม่มีชีวิตเหลือแล้ว 😭';
                        document.getElementById('gameLives').textContent = hearts;
                        
                        if (lives <= 0) {
                            gameActive = false;
                            ctx.fillStyle = 'rgba(0,0,0,0.6)';
                            ctx.fillRect(0, 0, gameCanvas.width, gameCanvas.height);
                            
                            ctx.fillStyle = '#ffffff';
                            ctx.font = 'bold 24px inherit';
                            ctx.textAlign = 'center';
                            ctx.fillText('เกมจบแล้วแง้ว! 😿', gameCanvas.width / 2, gameCanvas.height / 2 - 20);
                            
                            ctx.font = '16px inherit';
                            ctx.fillText(`คะแนนของพี่คนเก่ง: ${score} คะแนน`, gameCanvas.width / 2, gameCanvas.height / 2 + 15);
                            return;
                        }
                    }
                    
                    gameItems.splice(i, 1);
                    continue;
                }
                
                if (item.y > gameCanvas.height) {
                    gameItems.splice(i, 1);
                }
            }
            
            gameLoopId = requestAnimationFrame(gameLoop);
        }
        
        // --- Online Game: Iframe management ---
        window.loadOnlineGame = function(gameKey) {
            const iframe = document.getElementById('onlineGameIframe');
            const emptyState = document.getElementById('onlineGameEmptyState');
            if (!iframe || !emptyState) return;
            
            emptyState.classList.add('hidden');
            
            if (gameKey === '2048') {
                iframe.src = 'https://gabrielecirulli.github.io/2048/';
            } else if (gameKey === 'hextris') {
                iframe.src = 'https://hextris.io/';
            } else if (gameKey === 'pacman') {
                iframe.src = 'https://macek.github.io/google_pacman/';
            }
            showToast(`กำลังโหลดเกมออนไลน์... 🌐🎮`);
        };

        // ==================== GISX Automate (Register Case) Script ====================
        let gisxCases = [];
        let editingGisxIdx = -1;

        // Initialize GISX cases from localStorage
        try {
            const cached = localStorage.getItem('cat_gisx_cases');
            if (cached) {
                gisxCases = JSON.parse(cached);
            }
        } catch (e) {
            console.error('Failed to load cached GISX cases:', e);
        }

        window.downloadGisxTemplate = function () {
            showToast('กำลังดาวน์โหลดเทมเพลต Excel สำหรับ GISX... 📥');
            window.location.href = '/api/gisx/template';
        };

        // Setup Drag & Drop for GISX Upload Zone
        setTimeout(() => {
            const zone = document.getElementById('gisxUploadZone');
            if (!zone) return;

            zone.addEventListener('dragover', (e) => {
                e.preventDefault();
                zone.classList.add('bg-rose-50', 'border-rose-400');
            });

            zone.addEventListener('dragleave', () => {
                zone.classList.remove('bg-rose-50', 'border-rose-400');
            });

            zone.addEventListener('drop', (e) => {
                e.preventDefault();
                zone.classList.remove('bg-rose-50', 'border-rose-400');
                const files = e.dataTransfer.files;
                if (files.length > 0) {
                    uploadGisxFile(files[0]);
                }
            });

            zone.addEventListener('click', () => {
                document.getElementById('gisxFileInput').click();
            });

            renderGisxCases();
        }, 1000);

        window.handleGisxFileUpload = function (input) {
            if (input.files.length > 0) {
                uploadGisxFile(input.files[0]);
                input.value = ''; // Reset file input
            }
        };

        function uploadGisxFile(file) {
            if (!file.name.endsWith('.xlsx')) {
                alert('โปรดอัปโหลดไฟล์นามสกุล .xlsx เท่านั้นครับเมี้ยว! 😿');
                return;
            }

            const formData = new FormData();
            formData.append('file', file);

            showToast('กำลังวิเคราะห์และอัปโหลดไฟล์ Excel... 🐾');

            fetch('/api/gisx/upload', {
                method: 'POST',
                body: formData
            })
            .then(res => res.json())
            .then(data => {
                if (data.error) {
                    alert('เกิดข้อผิดพลาด: ' + data.error);
                    return;
                }

                gisxCases = data.cases || [];
                localStorage.setItem('cat_gisx_cases', JSON.stringify(gisxCases));
                
                // Show file info banner
                const fileInfo = document.getElementById('gisxFileInfo');
                const fileNameSpan = document.getElementById('gisxFileName');
                if (fileInfo && fileNameSpan) {
                    fileNameSpan.textContent = file.name;
                    fileInfo.classList.remove('hidden');
                }

                renderGisxCases();
                showToast('อัปโหลดข้อมูลเสร็จสิ้นและตรวจความครบถ้วนแล้วครับเหมียว! 🎉🐾');
                triggerAtmosphere();
            })
            .catch(err => {
                console.error(err);
                alert('ไม่สามารถประมวลผลไฟล์ได้: ' + err.message);
            });
        }

        window.clearGisxUpload = function () {
            document.getElementById('gisxFileInfo').classList.add('hidden');
            clearGisxCases();
        };

        window.clearGisxCases = function () {
            if (gisxCases.length > 0 && !confirm('คุณต้องการล้างข้อมูลเคสทั้งหมดในตารางใช่หรือไม่?')) return;
            gisxCases = [];
            localStorage.removeItem('cat_gisx_cases');
            renderGisxCases();
            showToast('ล้างตารางเรียบร้อยแล้ว 🧼');
        };

        window.deleteGisxCase = function (idx) {
            gisxCases.splice(idx, 1);
            localStorage.setItem('cat_gisx_cases', JSON.stringify(gisxCases));
            renderGisxCases();
            showToast('ลบรายการสำเร็จ! 🐾');
        };

        window.openEditGisxModal = function (idx) {
            editingGisxIdx = idx;
            const item = gisxCases[idx];
            if (!item) return;

            const fields = ['appNo', 'salesChannel', 'agentCode', 'title', 'firstName', 'lastName', 'gender', 'dob', 'occupation', 'plan', 'sumAssured', 'mode', 'premium', 'remark'];
            fields.forEach(f => {
                const el = document.getElementById('edit_' + f);
                if (el) {
                    el.value = item[f] !== undefined ? item[f] : '';
                }
            });

            document.getElementById('editGisxCaseModal').classList.remove('hidden');
        };

        window.closeEditGisxModal = function () {
            document.getElementById('editGisxCaseModal').classList.add('hidden');
            editingGisxIdx = -1;
        };

        window.saveEditGisxCase = function () {
            if (editingGisxIdx === -1) return;
            const item = gisxCases[editingGisxIdx];
            if (!item) return;

            const fields = ['appNo', 'salesChannel', 'agentCode', 'title', 'firstName', 'lastName', 'gender', 'dob', 'occupation', 'plan', 'sumAssured', 'mode', 'premium', 'remark'];
            fields.forEach(f => {
                const el = document.getElementById('edit_' + f);
                if (el) {
                    // Save as number if fields are numeric
                    if (f === 'sumAssured' || f === 'premium') {
                        item[f] = el.value ? parseFloat(el.value) : '';
                    } else {
                        item[f] = el.value.trim();
                    }
                }
            });

            // Re-validate fields
            const requiredFields = ['appNo', 'salesChannel', 'agentCode', 'firstName', 'lastName', 'gender', 'dob', 'plan', 'sumAssured', 'mode', 'premium'];
            const missing = [];
            requiredFields.forEach(f => {
                if (item[f] === undefined || item[f] === null || item[f] === '') {
                    missing.push(f);
                }
            });

            item.isValid = missing.length === 0;
            item.missingFields = missing;

            localStorage.setItem('cat_gisx_cases', JSON.stringify(gisxCases));
            closeEditGisxModal();
            renderGisxCases();
            showToast('บันทึกการแก้ไขเคสสำเร็จ! ✏️😻');
        };

        window.renderGisxCases = function () {
            const body = document.getElementById('gisxCasesBody');
            const statsSec = document.getElementById('gisxStatsSection');
            const casesSec = document.getElementById('gisxCasesSection');
            if (!body) return;

            body.innerHTML = '';

            if (gisxCases.length === 0) {
                statsSec.classList.add('hidden');
                casesSec.classList.add('hidden');
                return;
            }

            statsSec.classList.remove('hidden');
            casesSec.classList.remove('hidden');

            let validCount = 0;
            let invalidCount = 0;

            gisxCases.forEach((item, idx) => {
                if (item.isValid) validCount++;
                else invalidCount++;

                const tr = document.createElement('tr');
                tr.className = 'border-b border-rose-50 hover:bg-rose-50/20 transition-colors text-xs text-gray-700';

                // Columns construction
                const tdNum = document.createElement('td');
                tdNum.className = 'py-3 px-3 text-center font-bold text-gray-400';
                tdNum.textContent = idx + 1;
                tr.appendChild(tdNum);

                const tdAppNo = document.createElement('td');
                tdAppNo.className = 'py-3 px-3 font-semibold text-rose-500';
                tdAppNo.textContent = item.appNo || '(ไม่มีข้อมูล)';
                tr.appendChild(tdAppNo);

                const tdName = document.createElement('td');
                tdName.className = 'py-3 px-3';
                tdName.textContent = `${item.title || ''}${item.firstName || ''} ${item.lastName || ''}`.trim() || '(ไม่มีชื่อ)';
                tr.appendChild(tdName);

                const tdChannel = document.createElement('td');
                tdChannel.className = 'py-3 px-3';
                tdChannel.textContent = item.salesChannel || '-';
                tr.appendChild(tdChannel);

                const tdDob = document.createElement('td');
                tdDob.className = 'py-3 px-3 text-gray-500';
                tdDob.textContent = `${item.dob || '-'} (${item.gender || '-'})`;
                tr.appendChild(tdDob);

                const tdPlan = document.createElement('td');
                tdPlan.className = 'py-3 px-3 text-gray-600 font-medium truncate max-w-[150px]';
                tdPlan.title = item.plan || '-';
                tdPlan.textContent = item.plan || '-';
                tr.appendChild(tdPlan);

                const tdPremium = document.createElement('td');
                tdPremium.className = 'py-3 px-3 text-right font-bold text-gray-800';
                const premiumVal = parseFloat(item.premium);
                tdPremium.textContent = isNaN(premiumVal) ? '-' : premiumVal.toLocaleString('th-TH', { minimumFractionDigits: 2 });
                tr.appendChild(tdPremium);

                // Validation Status
                const tdStatus = document.createElement('td');
                tdStatus.className = 'py-3 px-3 text-center';
                if (item.isValid) {
                    tdStatus.innerHTML = '<span class="inline-flex items-center bg-green-50 text-green-700 px-2 py-1 rounded-full font-bold">ข้อมูลสมบูรณ์ ✔️</span>';
                } else {
                    const missingLabel = item.missingFields.join(', ');
                    tdStatus.innerHTML = `
                        <span class="inline-flex items-center bg-red-50 text-red-500 px-2 py-1 rounded-full font-bold cursor-help" title="ฟิลด์ที่ขาด: ${missingLabel}">
                            ข้อมูลไม่ครบ ✕ (ขาด: ${item.missingFields.length})
                        </span>
                    `;
                }
                tr.appendChild(tdStatus);

                // Actions cell
                const tdActions = document.createElement('td');
                tdActions.className = 'py-3 px-3 text-center flex items-center justify-center gap-1.5';
                tdActions.innerHTML = `
                    <button onclick="openEditGisxModal(${idx})" class="bg-amber-50 hover:bg-amber-100 text-amber-600 p-1.5 rounded-lg transition-colors font-bold text-[10px]" title="แก้ไขข้อมูล">
                        ✏️ แก้ไข
                    </button>
                    <button onclick="deleteGisxCase(${idx})" class="bg-rose-50 hover:bg-rose-100 text-rose-500 p-1.5 rounded-lg transition-colors font-bold text-[10px]" title="ลบเคส">
                        🗑️ ลบ
                    </button>
                `;
                tr.appendChild(tdActions);

                body.appendChild(tr);
            });

            // Update stats indicators
            document.getElementById('gisxStatTotal').textContent = gisxCases.length;
            document.getElementById('gisxStatValid').textContent = validCount;
            document.getElementById('gisxStatInvalid').textContent = invalidCount;
        };

        window.copyGisxBatchJson = function () {
            if (gisxCases.length === 0) return;
            const cleanCases = gisxCases.map(({ isValid, missingFields, rowNum, ...rest }) => rest);
            const jsonText = JSON.stringify(cleanCases, null, 2);

            navigator.clipboard.writeText(jsonText).then(() => {
                showToast('คัดลอก JSON Payloads ทั้งหมดของ GISX ลงคลิปบอร์ดแล้วครับ! 🐾📋');
            }).catch(err => {
                console.error(err);
                alert('ไม่สามารถคัดลอกได้: ' + err.message);
            });
        };

        window.downloadPlaywrightBatch = function () {
            if (gisxCases.length === 0) return;
            
            const cleanCases = gisxCases.map(({ isValid, missingFields, rowNum, ...rest }) => rest);
            const script = `/**
 * Playwright Automation Script for GISX Register Case Entry
 * Generated by KUMA Test Case Builder 🐾
 * 
 * Instructions:
 * 1. Install Playwright: npm install @playwright/test
 * 2. Save this file as 'gisx_register.spec.js'
 * 3. Run: npx playwright test gisx_register.spec.js
 */

const { test, expect } = require('@playwright/test');

test('Automate GISX Register Case Entry', async ({ page }) => {
    // Navigate to GISX Register Case URL
    await page.goto('https://gisx-qa.muangthai.co.th/new-business/register-case');
    
    // Login procedure (Uncomment and customize if login page appears)
    /*
    await page.fill('input[name="username"]', 'QA_USER');
    await page.fill('input[name="password"]', 'PASSWORD');
    await page.click('button[type="submit"]');
    await page.waitForNavigation();
    */

    const cases = ${JSON.stringify(cleanCases, null, 4)};

    for (let i = 0; i < cases.length; i++) {
        const item = cases[i];
        console.log(\`[KUMA] Registering case \${i + 1}/\${cases.length} - Application No: \${item.appNo}\`);

        // 1. Click "Add New" or "Register Case" button
        // await page.click('#btn-register-case');

        // 2. Fill Application Number
        await page.fill('input[name="appNo"]', item.appNo);

        // 3. Select Sales Channel (Dropdown)
        await page.selectOption('select[name="salesChannel"]', { label: item.salesChannel });

        // 4. Fill Agent Code
        await page.fill('input[name="agentCode"]', item.agentCode);

        // 5. Select Title (Dropdown - Optional)
        if (item.title) {
            await page.selectOption('select[name="title"]', { label: item.title });
        }

        // 6. Fill First Name & Last Name
        await page.fill('input[name="firstName"]', item.firstName);
        await page.fill('input[name="lastName"]', item.lastName);

        // 7. Select Gender (Dropdown)
        await page.selectOption('select[name="gender"]', { label: item.gender });

        // 8. Fill Date of Birth (YYYY-MM-DD)
        await page.fill('input[name="dob"]', item.dob);

        // 9. Select Occupation (Dropdown - Optional)
        if (item.occupation) {
            await page.selectOption('select[name="occupation"]', { label: item.occupation });
        }

        // 10. Select Insurance Plan (Dropdown)
        await page.selectOption('select[name="plan"]', { label: item.plan });

        // 11. Fill Sum Assured
        await page.fill('input[name="sumAssured"]', String(item.sumAssured));

        // 12. Select Premium Payment Mode (Dropdown)
        await page.selectOption('select[name="mode"]', { label: item.mode });

        // 13. Fill Premium Amount
        await page.fill('input[name="premium"]', String(item.premium));

        // 14. Fill Remark (Optional)
        if (item.remark) {
            await page.fill('input[name="remark"]', item.remark);
        }

        // 15. Click "Submit" or "Save" button to create policy
        // await page.click('button#save-case');
        // await page.waitForTimeout(2000); // Wait for API response
        
        console.log(\`[KUMA] Successfully registered Application No: \${item.appNo}\`);
    }
});
`;

            triggerDownload(script, 'gisx_playwright_test.js', 'text/javascript');
            showToast('ดาวน์โหลดสคริปต์ Playwright อัตโนมัติเรียบร้อยแล้วครับ 🟢🚀');
        };

        window.downloadRobotBatch = function () {
            if (gisxCases.length === 0) return;
            
            const cleanCases = gisxCases.map(({ isValid, missingFields, rowNum, ...rest }) => rest);
            
            // Build the Robot Framework calling cases
            let robotSteps = '';
            cleanCases.forEach(item => {
                robotSteps += `    Register Single Case    ${item.appNo}    ${item.salesChannel}    ${item.agentCode}    ${item.title || '${EMPTY}'}    ${item.firstName}    ${item.lastName}    ${item.gender}    ${item.dob}    ${item.occupation || '${EMPTY}'}    ${item.plan}    ${item.sumAssured}    ${item.mode}    ${item.premium}    ${item.remark || '${EMPTY}'}\n`;
            });

            const script = `*** Settings ***
Documentation     Automated GISX Case Registration Script
Library           SeleniumLibrary

*** Variables ***
\${URL}            https://gisx-qa.muangthai.co.th/new-business/register-case
\${BROWSER}        chrome

*** Test Cases ***
Register All Policy Cases From Excel
    [Documentation]    Iterates and enters policy case registrations automatically
    Open Browser    \${URL}    \${BROWSER}
    Maximize Browser Window
    # Run Login Procedure here if required...
    
\${robotSteps}
    [Teardown]    Close Browser

*** Keywords ***
Register Single Case
    [Arguments]    \${appNo}    \${salesChannel}    \${agentCode}    \${title}    \${firstName}    \${lastName}    \${gender}    \${dob}    \${occupation}    \${plan}    \${sumAssured}    \${mode}    \${premium}    \${remark}
    
    Log To Console    Registering Case: \${appNo} (\${firstName} \${lastName})
    
    # Click "Add Case" button
    # Click Element    xpath=//button[@id='add-new']
    # Sleep    1s

    # Input Fields selectors (please adjust XPath/CSS to match actual web fields)
    Input Text      xpath=//input[@name='appNo']            \${appNo}
    Select From List By Label    xpath=//select[@name='salesChannel']    \${salesChannel}
    Input Text      xpath=//input[@name='agentCode']         \${agentCode}
    Run Keyword If    '\${title}' != '\${EMPTY}'    Select From List By Label    xpath=//select[@name='title']    \${title}
    Input Text      xpath=//input[@name='firstName']         \${firstName}
    Input Text      xpath=//input[@name='lastName']          \${lastName}
    Select From List By Label    xpath=//select[@name='gender']          \${gender}
    Input Text      xpath=//input[@name='dob']               \${dob}
    Run Keyword If    '\${occupation}' != '\${EMPTY}'    Select From List By Label    xpath=//select[@name='occupation']    \${occupation}
    Select From List By Label    xpath=//select[@name='plan']            \${plan}
    Input Text      xpath=//input[@name='sumAssured']        \${sumAssured}
    Select From List By Label    xpath=//select[@name='mode']            \${mode}
    Input Text      xpath=//input[@name='premium']           \${premium}
    Run Keyword If    '\${remark}' != '\${EMPTY}'    Input Text      xpath=//input[@name='remark']           \${remark}

    # Click Save Button
    # Click Element    xpath=//button[@id='save']
    # Sleep    2s
`;

            const fullRobotFile = script.replace('\\${robotSteps}', robotSteps);
            triggerDownload(fullRobotFile, 'gisx_robot_test.robot', 'text/plain');
            showToast('ดาวน์โหลดสคริปต์ Robot Framework สำเร็จแล้วครับ 🤖📄');
        };

        function triggerDownload(content, filename, contentType) {
            const blob = new Blob([content], { type: contentType + ';charset=utf-8;' });
            const url = URL.createObjectURL(blob);
            const a = document.createElement('a');
            a.href = url;
            a.download = filename;
            document.body.appendChild(a);
            a.click();
            document.body.removeChild(a);
            URL.revokeObjectURL(url);
        }
    </script>
</body>

</html>