# 🐱 เครื่องมือสร้างกรณีทดสอบน้องแมว (Cat Test Case Builder)

ฅ^•ﻌ•^ฅ แอปพลิเคชันเว็บสำหรับสร้างกรณีทดสอบ (Test Case) และสร้าง JSON Payload แบบง่ายๆ พร้อมส่งออกเป็นไฟล์ Excel

---

## 🚀 เริ่มใช้งานทันที (Windows)

หากคุณใช้ Windows สามารถกดรันไฟล์นี้ได้เลย:
- **`Start_Cat_App.bat`** (ดับเบิลคลิกเพื่อเริ่มระบบและเปิดเบราว์เซอร์อัตโนมัติ)

---

## 📦 การติดตั้ง (Manual)

```bash
# 1. ติดตั้ง Dependencies
npm install

# 2. เริ่มเซิร์ฟเวอร์
npm run dev
```

เปิดเบราว์เซอร์ไปที่ **http://localhost:3000**

---

## 🎮 วิธีใช้งาน

### 📝 หน้า 1: สร้างกรณีทดสอบ (Test Case Builder)

| ช่อง | คำอธิบาย |
|------|----------|
| **ชื่อกรณีทดสอบ** | ชื่อหลักของชุดทดสอบ เช่น "การล็อกอิน" |
| **ขั้นตอนการทดสอบ** | อธิบายสิ่งที่ต้องทำในขั้นตอนนี้ |
| **ผลลัพธ์ที่คาดหวัง** | ผลลัพธ์ที่ควรเกิดขึ้นหลังทำขั้นตอนนั้น |

1. กรอกข้อมูลครบทุกช่อง → ปุ่ม "เพิ่มขั้นตอนถัดไป" จะใช้งานได้
2. คลิกเพิ่ม → ข้อมูลจะลงตาราง, ช่อง "ขั้นตอน" และ "ผลลัพธ์" ล้างอัตโนมัติ
3. กด ✕ เพื่อลบทีละแถว หรือ "ล้างทั้งหมด" เพื่อรีเซ็ต
4. กด **"ส่งออกเป็น Excel"** เพื่อดาวน์โหลด `.xlsx` (รองรับตัดบรรทัดตาม textarea ที่ใส่)

---

### 🔧 หน้า 2: JSON Payload Generator

ฟีเจอร์สำหรับสร้าง JSON Payload จากไฟล์ Excel

#### ขั้นตอนการใช้งาน:

1. **ดาวน์โหลดไฟล์ตัวอย่าง** → คลิกปุ่ม "ดาวน์โหลดไฟล์ตัวอย่าง" จะได้ไฟล์ Excel ที่มีคอลัมน์:

   | No. | Key | Value | Description |
   |-----|-----|-------|-------------|
   | 1 | username | testuser01 | ชื่อผู้ใช้ |
   | 2 | password | P@ssw0rd | รหัสผ่าน |

2. **แก้ไขไฟล์** → ใส่ Key-Value ที่ต้องการลงใน Excel แล้วบันทึก

3. **อัปโหลดกลับมา** → ลากไฟล์มาวาง หรือคลิกเลือกไฟล์ `.xlsx`

4. **ดูตัวอย่าง** → โปรแกรมจะสร้าง JSON Payload และแสดงเป็นข้อๆ พร้อม:
   - ตาราง Key-Value
   - JSON Preview พร้อม syntax highlighting
   - ปุ่มคัดลอก

5. **ส่งออก** → กด "ส่งออกเป็น Excel" เพื่อดาวน์โหลดไฟล์รวม Payload ทั้งหมด

---

## 🛠 เทคโนโลยีที่ใช้

- **Node.js** + **Express.js** — เซิร์ฟเวอร์
- **EJS** — Template Engine
- **Tailwind CSS** — สไตล์ UI
- **ExcelJS** — สร้างและอ่านไฟล์ Excel (รองรับ Wrap Text)
- **FileSaver.js** — ดาวน์โหลดไฟล์ฝั่ง Client

---

## 📁 โครงสร้างโปรเจกต์

```
├── server.js          # Express server + routes + template API
├── views/
│   └── index.ejs      # หน้าเว็บหลัก (2 หน้า + Client-side JS)
├── package.json
└── README.md          # คู่มือการใช้งาน (ไฟล์นี้)
```
